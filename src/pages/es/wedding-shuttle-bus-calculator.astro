---
import Layout from '../../layouts/Layout.astro';
import RelatedTools from '../../components/RelatedTools.astro';

const jsonLd = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebApplication",
      "name": "Calculadora de Autobuses y Transporte de Invitados para Bodas",
      "url": "https://manage.wedding/es/wedding-shuttle-bus-calculator",
      "applicationCategory": "UtilityApplication",
      "operatingSystem": "All",
      "browserRequirements": "Requires JavaScript. Requires HTML5.",
      "description": "Calcula cuántos autobuses o minibuses necesitas para trasladar a los invitados de la boda, horarios de rutas de ida y vuelta, propinas y costes.",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD"
      }
    }
  ]
};
---

<Layout
	title="Calculadora de Autobuses para Bodas — Plazas, Rutas y Costes"
	description="Calcula cuántos autobuses o minibuses contratar para el transporte de invitados a la boda, frecuencias de viaje, propinas del conductor y presupuesto."
	lang="es"
	schema={jsonLd}
>
	<nav class="breadcrumb-nav" aria-label="Miga de pan" style="max-width: 1200px; margin: 1rem auto 0; padding: 0 1rem; font-size: 0.88rem;">
		<ol style="list-style: none; display: flex; flex-wrap: wrap; gap: 0.5rem; padding: 0; margin: 0; color: var(--text-secondary);">
			<li><a href="/es/" style="color: var(--text-secondary); text-decoration: none;">Inicio</a></li>
			<li><span style="color: var(--text-muted);">&rsaquo;</span></li>
			<li><a href="/es/guests" style="color: var(--text-secondary); text-decoration: none;">Invitados y Logística</a></li>
			<li><span style="color: var(--text-muted);">&rsaquo;</span></li>
			<li aria-current="page" style="color: var(--fg-pencil); font-weight: 700;">Calculadora de Autobuses</li>
		</ol>
	</nav>

	<main style="max-width: 1200px; margin: 0 auto; padding: 1rem 1rem 3.5rem;">
		
		<!-- Hero Section -->
		<div style="text-align: center; margin-bottom: 2rem;">
			<div class="tape" style="margin: 0 auto 0.75rem;"></div>
			<div style="display: inline-flex; align-items: center; gap: 0.5rem; background: var(--post-it); border: 2px solid var(--fg-pencil); border-radius: 999px; padding: 4px 14px; margin-bottom: 0.75rem; box-shadow: 2px 2px 0px var(--fg-pencil);">
				<span>🚌</span>
				<span style="font-size: 0.85rem; font-weight: 700; color: var(--fg-pencil);">Transporte de Invitados, Rutas y Presupuesto</span>
			</div>
			<h1 class="font-heading" style="font-size: clamp(1.85rem, 4vw, 2.75rem); color: var(--fg-pencil); margin: 0.25rem 0 0.75rem; line-height: 1.15;">
				Calculadora de Autobuses para Bodas
			</h1>
			<p class="font-body" style="font-size: 1.05rem; color: var(--text-secondary); max-width: 780px; margin: 0 auto; line-height: 1.5;">
				Calcula <strong>cuántos autobuses o minibuses necesitas</strong>, capacidades de vehículos (55 plazas, microbús 28 plazas), tiempos de rotación y <strong>costes totales con conductor</strong>.
			</p>
		</div>

		<!-- Interactive Calculator Grid -->
		<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 1.5rem; align-items: start; margin-bottom: 2.5rem;">
			
			<!-- Left Column: Inputs -->
			<div class="hand-card" style="padding: 1.5rem; background: #fff;">
				<h2 class="font-heading" style="font-size: 1.25rem; color: var(--fg-pencil); margin: 0 0 1.25rem; display: flex; align-items: center; gap: 0.5rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.5rem;">
					<span>👥</span> 1. Invitados y Uso de Autobús
				</h2>

				<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem; margin-bottom: 1.25rem;">
					<div>
						<label for="input-total-guests" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Total Invitados Boda:</label>
						<input type="number" id="input-total-guests" min="20" max="500" value="140" style="width: 100%; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-weight: 700;" />
					</div>
					<div>
						<label for="select-shuttle-rate" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">% que usa el Autobús:</label>
						<select id="select-shuttle-rate" style="width: 100%; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
							<option value="0.50">50% (Muchos van en su coche)</option>
							<option value="0.65" selected>65% (Media habitual en bodas)</option>
							<option value="0.80">80% (Hotel/Finca alejada)</option>
							<option value="0.95">95% (Sin aparcamiento en la finca)</option>
						</select>
					</div>
				</div>

				<h2 class="font-heading" style="font-size: 1.15rem; color: var(--fg-pencil); margin: 1.25rem 0 0.75rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.4rem;">
					🛣️ 2. Trayecto y Tipo de Vehículo
				</h2>

				<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem; margin-bottom: 1rem;">
					<div>
						<label for="select-vehicle-type" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Tipo de Vehículo:</label>
						<select id="select-vehicle-type" style="width: 100%; padding: 0.45rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
							<option value="56-coach" selected>🚌 Autocar Grande (55 plazas)</option>
							<option value="28-minibus">🚐 Minibús / Microbús (28 plazas)</option>
							<option value="14-sprinter">🚐 Minivan VIP (14 plazas)</option>
						</select>
					</div>
					<div>
						<label for="input-drive-time" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Tiempo Trayecto Solo Ida (min):</label>
						<input type="number" id="input-drive-time" min="5" max="60" value="15" style="width: 100%; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem;" />
					</div>
				</div>

				<h2 class="font-heading" style="font-size: 1.15rem; color: var(--fg-pencil); margin: 1.25rem 0 0.75rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.4rem;">
					⏰ 3. Horas de Servicio y Viaje Temprano
				</h2>

				<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem;">
					<div>
						<label for="select-charter-hours" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Horas Totales del Servicio:</label>
						<select id="select-charter-hours" style="width: 100%; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
							<option value="5">5 Horas (Solo traslado y vuelta)</option>
							<option value="7" selected>7 Horas (Ceremonia a fin de fiesta)</option>
							<option value="9">9 Horas (Día completo + After)</option>
						</select>
					</div>
					<div>
						<label for="select-early-run" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">¿Viaje Temprano de Regreso?</label>
						<select id="select-early-run" style="width: 100%; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
							<option value="yes" selected>Sí (Para mayores y familias con niños)</option>
							<option value="no">No (Todos regresan al final)</option>
						</select>
					</div>
				</div>

			</div>

			<!-- Right Column: Results -->
			<div class="hand-card" style="padding: 1.5rem; background: #fffdfa; border-color: var(--fg-pencil); position: sticky; top: 1rem;">
				<div style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 1rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.75rem;">
					<div>
						<span style="font-size: 0.75rem; text-transform: uppercase; font-weight: 700; letter-spacing: 0.05em; color: var(--text-secondary);">Plan de Flota Recomendado</span>
						<h2 class="font-heading" style="font-size: 1.4rem; color: var(--fg-pencil); margin: 0;">
							Flota Necesaria
						</h2>
					</div>
					<span id="badge-shuttle-riders" style="background: var(--post-it); border: 1.5px solid var(--fg-pencil); font-size: 0.75rem; font-weight: 700; padding: 2px 8px; border-radius: 4px;">91 Pasajeros</span>
				</div>

				<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem; margin-bottom: 1.25rem;">
					<div style="background: #fff; border: 2px solid var(--fg-pencil); border-radius: 8px; padding: 0.85rem; text-align: center; box-shadow: 3px 3px 0px var(--fg-pencil);">
						<span style="font-size: 0.75rem; font-weight: 700; color: var(--text-secondary); text-transform: uppercase; display: block;">Vehículos Recomendados</span>
						<strong id="stat-vehicles-needed" style="font-size: 1.8rem; font-weight: 900; color: #b91c1c; line-height: 1.1;">2 Autocares</strong>
						<span id="stat-vehicles-sub" style="font-size: 0.75rem; color: #64748b; display: block; margin-top: 2px;">(Autocar de 55 plazas)</span>
					</div>

					<div style="background: #fff; border: 2px solid var(--fg-pencil); border-radius: 8px; padding: 0.85rem; text-align: center; box-shadow: 3px 3px 0px var(--fg-pencil);">
						<span style="font-size: 0.75rem; font-weight: 700; color: var(--text-secondary); text-transform: uppercase; display: block;">Coste Estimado</span>
						<strong id="stat-total-cost" style="font-size: 1.8rem; font-weight: 900; color: #1e40af; line-height: 1.1;">2.150 €</strong>
						<span id="stat-cost-sub" style="font-size: 0.75rem; color: #64748b; display: block; margin-top: 2px;">Incluye propina/dietas conductor</span>
					</div>
				</div>

				<div style="background: #f0fdf4; border: 1.5px solid #16a34a; border-radius: 6px; padding: 0.85rem; margin-bottom: 1.25rem; font-size: 0.85rem;">
					<strong style="color: #166534; display: block; margin-bottom: 0.35rem;">⏱️ Horarios de Ruta y Rotación:</strong>
					<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.5rem; color: #14532d;">
						<div>• <strong id="stat-roundtrip-time">40 min</strong> Trayecto Ida y Vuelta</div>
						<div>• <strong id="stat-arrival-runs">1 Viaje por bus</strong> (Llegan juntos)</div>
						<div>• <strong id="stat-early-departure">21:30</strong> Viaje de Regreso Temprano</div>
						<div>• <strong id="stat-final-departure">02:30</strong> Regreso Fin de Fiesta</div>
					</div>
				</div>

				<div style="background: #f8fafc; border: 1.5px solid var(--fg-pencil); border-radius: 6px; padding: 0.85rem; margin-bottom: 1.25rem; font-size: 0.88rem;">
					<div style="display: flex; justify-content: space-between; padding: 0.25rem 0; border-bottom: 1px dashed #cbd5e1;">
						<span style="color: #475569;">🚍 Tarifa Base Alquiler:</span>
						<strong id="stat-base-charter" style="color: var(--fg-pencil);">1.850 € (7h @ 135 €/h/bus)</strong>
					</div>
					<div style="display: flex; justify-content: space-between; padding: 0.25rem 0; border-bottom: 1px dashed #cbd5e1;">
						<span style="color: #475569;">💵 Dieta / Propina Conductor:</span>
						<strong id="stat-driver-tip" style="color: #b91c1c;">300 € (150 € por conductor)</strong>
					</div>
					<div style="display: flex; justify-content: space-between; padding: 0.25rem 0;">
						<span style="color: #475569;">👥 Coste por Invitado:</span>
						<strong id="stat-cost-per-head" style="color: #15803d;">~23,60 € por persona</strong>
					</div>
				</div>

				<div style="display: flex; flex-wrap: wrap; gap: 0.5rem;">
					<button type="button" id="btn-print-summary" class="hand-btn" style="flex: 1; background: var(--accent-red); color: #fff; padding: 0.6rem 1rem; font-size: 0.9rem; font-weight: 700; border: 2px solid var(--fg-pencil); border-radius: 6px; cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 0.4rem; box-shadow: 2px 2px 0px var(--fg-pencil);">
						<span>🖨️</span> Imprimir Hoja de Ruta (PDF)
					</button>
					<button type="button" id="btn-export-doc" class="hand-btn" style="flex: 1; background: #fff; color: var(--fg-pencil); padding: 0.6rem 1rem; font-size: 0.9rem; font-weight: 700; border: 2px solid var(--fg-pencil); border-radius: 6px; cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 0.4rem; box-shadow: 2px 2px 0px var(--fg-pencil);">
						<span>📄</span> Exportar Word (.doc)
					</button>
				</div>
			</div>

		</div>

		<!-- Related Tools Component -->
		<RelatedTools currentPath="/es/wedding-shuttle-bus-calculator" category="guests" />

	</main>

	<!-- Hidden Print Containers for Vector PDF / Window.print -->
	<div id="print-root" class="print-only">
		<div id="print-shuttle-container" class="print-page" style="padding: 10mm;">
			<div style="border-bottom: 2pt solid #000; padding-bottom: 4mm; margin-bottom: 6mm;">
				<h1 style="font-size: 20pt; margin: 0 0 3mm 0; color: #000;">Hoja de Ruta y Logística de Autobuses para Boda</h1>
				<p id="print-shuttle-meta" style="font-size: 10.5pt; color: #333; margin: 0;">Plan de Transporte de Invitados | Manage.Wedding</p>
			</div>
			<div id="print-shuttle-tables">
			</div>
		</div>
	</div>
</Layout>

<style>
	.hand-card {
		background: #fff;
		border: 2px solid var(--fg-pencil);
		border-radius: 8px;
		box-shadow: 4px 4px 0px var(--fg-pencil);
	}

	.badge-postit {
		background: var(--post-it);
		border: 1.5px solid var(--fg-pencil);
		box-shadow: 2px 2px 0px var(--fg-pencil);
	}
</style>

<style is:global>
	.print-only {
		display: none;
	}

	@media print {
		nav,
		footer,
		.nav-sketch,
		.site-footer,
		.site-header,
		.nav-links-desktop,
		.lang-dropdown-wrapper,
		.hamburger-btn,
		.no-print,
		main,
		.breadcrumb-nav {
			display: none !important;
		}

		.print-only,
		#print-root {
			display: block !important;
			visibility: visible !important;
			position: static !important;
			width: 100% !important;
			background: #fff !important;
			color: #000 !important;
		}

		#print-root * {
			visibility: visible !important;
		}

		.print-page {
			page-break-after: always !important;
			break-after: page !important;
		}
	}
</style>

<script is:inline>
	const inputTotalGuests = document.getElementById('input-total-guests');
	const selectShuttleRate = document.getElementById('select-shuttle-rate');
	const selectVehicleType = document.getElementById('select-vehicle-type');
	const inputDriveTime = document.getElementById('input-drive-time');
	const selectCharterHours = document.getElementById('select-charter-hours');
	const selectEarlyRun = document.getElementById('select-early-run');

	const badgeShuttleRiders = document.getElementById('badge-shuttle-riders');
	const statVehiclesNeeded = document.getElementById('stat-vehicles-needed');
	const statVehiclesSub = document.getElementById('stat-vehicles-sub');
	const statTotalCost = document.getElementById('stat-total-cost');
	const statCostSub = document.getElementById('stat-cost-sub');

	const statRoundtripTime = document.getElementById('stat-roundtrip-time');
	const statArrivalRuns = document.getElementById('stat-arrival-runs');
	const statEarlyDeparture = document.getElementById('stat-early-departure');
	const statFinalDeparture = document.getElementById('stat-final-departure');

	const statBaseCharter = document.getElementById('stat-base-charter');
	const statDriverTip = document.getElementById('stat-driver-tip');
	const statCostPerHead = document.getElementById('stat-cost-per-head');

	function calculateShuttle() {
		const totalGuests = Math.max(10, parseInt(inputTotalGuests.value, 10) || 140);
		const shuttleRate = parseFloat(selectShuttleRate.value) || 0.65;
		const riders = Math.round(totalGuests * shuttleRate);
		const driveTime = Math.max(5, parseInt(inputDriveTime.value, 10) || 15);
		const charterHours = parseInt(selectCharterHours.value, 10) || 7;
		const vehicleType = selectVehicleType.value;
		const hasEarly = selectEarlyRun.value === 'yes';

		badgeShuttleRiders.textContent = `${riders} Pasajeros`;

		const roundtripMins = (driveTime * 2) + 10;
		statRoundtripTime.textContent = `${roundtripMins} min Ida y Vuelta`;

		let capacity = 55;
		let hourlyRate = 135;
		let vehicleLabel = 'Autocar de 55 plazas';

		if (vehicleType === '56-coach') {
			capacity = 55;
			hourlyRate = 135;
			vehicleLabel = 'Autocar de 55 plazas';
		} else if (vehicleType === '28-minibus') {
			capacity = 28;
			hourlyRate = 110;
			vehicleLabel = 'Microbús de 28 plazas';
		} else {
			capacity = 14;
			hourlyRate = 85;
			vehicleLabel = 'Minivan VIP de 14 plazas';
		}

		let loopsPossible = 1;
		if (roundtripMins <= 35) loopsPossible = 2;

		const capacityPerVehicle = capacity * loopsPossible;
		const vehiclesNeeded = Math.max(1, Math.ceil(riders / capacityPerVehicle));

		statVehiclesNeeded.textContent = `${vehiclesNeeded} ${vehiclesNeeded > 1 ? 'Autocares' : 'Autocar'}`;
		statVehiclesSub.textContent = `(${vehicleLabel})`;

		if (loopsPossible === 1) {
			statArrivalRuns.textContent = '1 Viaje por bus (Llegan juntos)';
		} else {
			statArrivalRuns.textContent = '2 Turnos escalonados por bus';
		}

		statEarlyDeparture.textContent = hasEarly ? '21:30 Viaje Temprano' : 'Sin turno temprano';
		statFinalDeparture.textContent = '02:30 Regreso Fin de Fiesta';

		const baseCost = vehiclesNeeded * charterHours * hourlyRate;
		const driverTip = Math.round(baseCost * 0.15);
		const totalCost = baseCost + driverTip;

		statBaseCharter.textContent = `${baseCost.toLocaleString()} € (${charterHours}h @ ${hourlyRate} €/h/bus)`;
		statDriverTip.textContent = `${driverTip.toLocaleString()} € (${Math.round(driverTip / vehiclesNeeded)} € por chófer)`;
		statTotalCost.textContent = `${totalCost.toLocaleString()} €`;
		statCostSub.textContent = 'Incluye propina/dietas conductor';
		statCostPerHead.textContent = `~${(totalCost / riders).toFixed(2)} € por pasajero`;

		document.getElementById('print-shuttle-tables').innerHTML = `
			<table style="width:100%; border-collapse:collapse; font-size:10pt; text-align:left; margin-top:10px;">
				<thead>
					<tr style="background:#e2e8f0; border-bottom:2pt solid #000;">
						<th style="padding:6pt;">Elemento de Transporte</th>
						<th style="padding:6pt;">Detalles Logísticos</th>
						<th style="padding:6pt; text-align:right;">Planificación</th>
					</tr>
				</thead>
				<tbody>
					<tr style="border-bottom:1pt solid #cbd5e1;">
						<td style="padding:6pt; font-weight:bold;">Total Pasajeros Autobús</td>
						<td style="padding:6pt;">${totalGuests} invitados x ${Math.round(shuttleRate * 100)}% de uso</td>
						<td style="padding:6pt; text-align:right;"><strong>${riders} Pasajeros</strong></td>
					</tr>
					<tr style="border-bottom:1pt solid #cbd5e1;">
						<td style="padding:6pt; font-weight:bold;">Flota Contratada</td>
						<td style="padding:6pt;">${vehicleLabel}</td>
						<td style="padding:6pt; text-align:right; font-weight:bold; color:#b91c1c;">${vehiclesNeeded} ${vehiclesNeeded > 1 ? 'Vehículos' : 'Vehículo'}</td>
					</tr>
					<tr style="border-bottom:1pt solid #cbd5e1;">
						<td style="padding:6pt; font-weight:bold;">Tiempo de Rotación</td>
						<td style="padding:6pt;">${driveTime} min trayecto + 10 min subida/bajada</td>
						<td style="padding:6pt; text-align:right;">${roundtripMins} min Ida y Vuelta</td>
					</tr>
					<tr style="border-bottom:1pt solid #cbd5e1;">
						<td style="padding:6pt; font-weight:bold;">Horarios de Vuelta</td>
						<td style="padding:6pt;">${hasEarly ? 'Vuelta temprana 21:30 y ' : ''}Fin de fiesta 02:30</td>
						<td style="padding:6pt; text-align:right;">${hasEarly ? '2 Turnos' : '1 Turno'}</td>
					</tr>
					<tr style="background:#eff6ff; font-weight:bold; font-size:11pt; border-top:2pt solid #000;">
						<td style="padding:8pt;">COSTE TOTAL ESTIMADO</td>
						<td style="padding:8pt;">${charterHours} Horas de servicio (${vehiclesNeeded} chóferes con dieta)</td>
						<td style="padding:8pt; text-align:right; color:#1e40af;">${totalCost.toLocaleString()} €</td>
					</tr>
				</tbody>
			</table>
		`;
	}

	[inputTotalGuests, selectShuttleRate, selectVehicleType, inputDriveTime, selectCharterHours, selectEarlyRun].forEach(el => {
		el.addEventListener('input', calculateShuttle);
		el.addEventListener('change', calculateShuttle);
	});

	document.getElementById('btn-print-summary').addEventListener('click', () => {
		calculateShuttle();
		window.print();
	});

	document.getElementById('btn-export-doc').addEventListener('click', () => {
		calculateShuttle();
		const fleet = statVehiclesNeeded.textContent;
		const cost = statTotalCost.textContent;
		const riders = badgeShuttleRiders.textContent;
		const guests = inputTotalGuests.value;

		const html = `
			<html xmlns:o='urn:schemas-microsoft-com:office:office' xmlns:w='urn:schemas-microsoft-com:office:word' xmlns='http://www.w3.org/TR/REC-html40'>
			<head><meta charset='utf-8'><title>Transporte Autobuses Boda</title>
			<style>body{font-family:Arial,sans-serif;line-height:1.4;} table{border-collapse:collapse;width:100%;} th,td{padding:6pt;border:1pt solid #ccc;text-align:left;}</style>
			</head>
			<body>
				<h1>Hoja de Ruta y Autobuses para Boda</h1>
				<p><strong>Invitados:</strong> ${guests} | <strong>Pasajeros:</strong> ${riders} | <strong>Flota:</strong> ${fleet} | <strong>Coste:</strong> ${cost}</p>
				<hr/>
				<p><em>Generado por Manage.Wedding</em></p>
			</body>
			</html>
		`;

		const blob = new Blob(['\ufeff', html], { type: 'application/msword' });
		const url = URL.createObjectURL(blob);
		const a = document.createElement('a');
		a.href = url;
		a.download = `Autobuses-Boda-${new Date().toISOString().slice(0,10)}.doc`;
		document.body.appendChild(a);
		a.click();
		document.body.removeChild(a);
		URL.revokeObjectURL(url);
	});

	calculateShuttle();
</script>
