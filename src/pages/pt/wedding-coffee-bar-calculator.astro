---
import Layout from '../../layouts/Layout.astro';
import RelatedTools from '../../components/RelatedTools.astro';

const jsonLd = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebApplication",
      "name": "Calculadora de Coffee Bar e Estação de Café para Casamento",
      "url": "https://manage.wedding/pt/wedding-coffee-bar-calculator",
      "applicationCategory": "UtilityApplication",
      "operatingSystem": "All",
      "browserRequirements": "Requires JavaScript. Requires HTML5.",
      "description": "Calcule a quantidade de café em grãos (kg), proporção descafeinado, litros de leite integral e vegetal (aveia), xaropes, copos e custos de barista.",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD"
      }
    }
  ]
};
---

<Layout
	title="Calculadora de Coffee Bar para Casamento — Grãos, Leite & Carrinho"
	description="Calcule quantos kg de café em grãos comprar para o casamento, proporção café normal e descafeinado, leite de aveia, xaropes, copos e garrafas térmicas."
	lang="pt"
	schema={jsonLd}
>
	<nav class="breadcrumb-nav" aria-label="Breadcrumb" style="max-width: 1200px; margin: 1rem auto 0; padding: 0 1rem; font-size: 0.88rem;">
		<ol style="list-style: none; display: flex; flex-wrap: wrap; gap: 0.5rem; padding: 0; margin: 0; color: var(--text-secondary);">
			<li><a href=""" style="color: var(--text-secondary); text-decoration: none;">Início</a></li>
			<li><span style="color: var(--text-muted);">&rsaquo;</span></li>
			<li><a href="/pt/dining" style="color: var(--text-secondary); text-decoration: none;">Buffet e Bebidas</a></li>
			<li><span style="color: var(--text-muted);">&rsaquo;</span></li>
			<li aria-current="page" style="color: var(--fg-pencil); font-weight: 700;">Coffee Bar</li>
		</ol>
	</nav>

	<main style="max-width: 1200px; margin: 0 auto; padding: 1rem 1rem 3.5rem;">
		
		<!-- Hero Section -->
		<div style="text-align: center; margin-bottom: 2rem;">
			<div class="tape" style="margin: 0 auto 0.75rem;"></div>
			<div style="display: inline-flex; align-items: center; gap: 0.5rem; background: var(--post-it); border: 2px solid var(--fg-pencil); border-radius: 999px; padding: 4px 14px; margin-bottom: 0.75rem; box-shadow: 2px 2px 0px var(--fg-pencil);">
				<span>☕</span>
				<span style="font-size: 0.85rem; font-weight: 700; color: var(--fg-pencil);">Café Especial em Grãos, Leites Vegetais & Barista DIY</span>
			</div>
			<h1 class="font-heading" style="font-size: clamp(1.85rem, 4vw, 2.75rem); color: var(--fg-pencil); margin: 0.25rem 0 0.75rem; line-height: 1.15;">
				Calculadora de Coffee Bar para Casamento
			</h1>
			<p class="font-body" style="font-size: 1.05rem; color: var(--text-secondary); max-width: 780px; margin: 0 auto; line-height: 1.5;">
				Calcule a <strong>quantidade de café em grãos (kg), proporção descafeinado</strong>, litros de leite integral e vegetal (aveia), xaropes, copos e compare <strong>estação DIY vs Carrinho de Barista</strong>.
			</p>
		</div>

		<!-- Interactive Calculator Grid -->
		<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 1.5rem; align-items: start; margin-bottom: 2.5rem;">
			
			<!-- Left Column: Inputs -->
			<div class="hand-card" style="padding: 1.5rem; background: #fff;">
				<h2 class="font-heading" style="font-size: 1.25rem; color: var(--fg-pencil); margin: 0 0 1.25rem; display: flex; align-items: center; gap: 0.5rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.5rem;">
					<span>👥</span> 1. Convidados e Formato do Café
				</h2>

				<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem; margin-bottom: 1.25rem;">
					<div>
						<label for="input-coffee-guests" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Total de Convidados:</label>
						<input type="number" id="input-coffee-guests" min="20" max="500" value="130" style="width: 100%; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-weight: 700;" />
					</div>
					<div>
						<label for="select-coffee-setup" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Tipo de Estação:</label>
						<select id="select-coffee-setup" style="width: 100%; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
							<option value="diy-urn" selected>🫖 Garrafas Térmicas & Cafeteiras Percoladoras DIY</option>
							<option value="espresso-cart">☕ Carrinho de Café Gourmet com Barista & Latte Art</option>
							<option value="drip-caterer">🍽️ Serviço Tradicional de Café do Buffet</option>
							<option value="iced-bar">🧊 Bar de Cold Brew & Café Gelado</option>
						</select>
					</div>
				</div>

				<h2 class="font-heading" style="font-size: 1.15rem; color: var(--fg-pencil); margin: 1.25rem 0 0.75rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.4rem;">
					🕒 2. Horário do Serviço e Estação
				</h2>

				<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem; margin-bottom: 1rem;">
					<div>
						<label for="select-coffee-timing" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Momento do Serviço:</label>
						<select id="select-coffee-timing" style="width: 100%; padding: 0.45rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
							<option value="evening" selected>Noite com Bolo / Doces (70% Normal / 30% Descafeinado)</option>
							<option value="late-night">Madrugada / Energia Pista (85% Normal)</option>
							<option value="brunch">Brunch / Casamento Diurno (90% Normal)</option>
						</select>
					</div>
					<div>
						<label for="select-coffee-season" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Clima / Estação:</label>
						<select id="select-coffee-season" style="width: 100%; padding: 0.45rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
							<option value="fall-winter" selected>🍂 Outono / Inverno (Alta demanda: 75% tomam café)</option>
							<option value="spring-summer">☀️ Primavera / Verão (Demanda moderada: 55%)</option>
						</select>
					</div>
				</div>

				<h2 class="font-heading" style="font-size: 1.15rem; color: var(--fg-pencil); margin: 1.25rem 0 0.75rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.4rem;">
					🍯 3. Xaropes Gourmet e Copos
				</h2>

				<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem;">
					<div>
						<label for="select-syrup-tier" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Xaropes Aromatizantes:</label>
						<select id="select-syrup-tier" style="width: 100%; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
							<option value="standard" selected>Trio Clássico (Baunilha, Caramelo, Avelã)</option>
							<option value="deluxe">Kit Deluxe 5 Sabores (Canela, Mocha, Chai)</option>
							<option value="none">Simples (Apenas açúcar e leite)</option>
						</select>
					</div>
					<div>
						<label for="select-cup-type" style="display: block; font-size: 0.82rem; font-weight: 700; color: #334155; margin-bottom: 0.2rem;">Tipo de Copos:</label>
						<select id="select-cup-type" style="width: 100%; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
							<option value="insulated" selected>Copos Térmicos Personalizados com Tampa</option>
							<option value="ceramic">Xícaras de Porcelana Alugadas</option>
							<option value="clear-iced">Copos Transparentes com Canudo (Café Gelado)</option>
						</select>
					</div>
				</div>

			</div>

			<!-- Right Column: Results -->
			<div class="hand-card" style="padding: 1.5rem; background: #fffdfa; border-color: var(--fg-pencil); position: sticky; top: 1rem;">
				<div style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 1rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.75rem;">
					<div>
						<span style="font-size: 0.75rem; text-transform: uppercase; font-weight: 700; letter-spacing: 0.05em; color: var(--text-secondary);">Previsão de Suprimentos</span>
						<h2 class="font-heading" style="font-size: 1.4rem; color: var(--fg-pencil); margin: 0;">
							Café e Insumos
						</h2>
					</div>
					<span id="badge-total-cups" style="background: var(--post-it); border: 1.5px solid var(--fg-pencil); font-size: 0.75rem; font-weight: 700; padding: 2px 8px; border-radius: 4px;">135 Xícaras</span>
				</div>

				<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem; margin-bottom: 1.25rem;">
					<div style="background: #fff; border: 2px solid var(--fg-pencil); border-radius: 8px; padding: 0.85rem; text-align: center; box-shadow: 3px 3px 0px var(--fg-pencil);">
						<span style="font-size: 0.75rem; font-weight: 700; color: var(--text-secondary); text-transform: uppercase; display: block;">Café em Grãos</span>
						<strong id="stat-coffee-lbs" style="font-size: 1.8rem; font-weight: 900; color: #b91c1c; line-height: 1.1;">2,1 kg</strong>
						<span id="stat-coffee-sub" style="font-size: 0.75rem; color: #64748b; display: block; margin-top: 2px;">(1,5 kg Tradicional / 0,6 kg Descafeinado)</span>
					</div>

					<div style="background: #fff; border: 2px solid var(--fg-pencil); border-radius: 8px; padding: 0.85rem; text-align: center; box-shadow: 3px 3px 0px var(--fg-pencil);">
						<span style="font-size: 0.75rem; font-weight: 700; color: var(--text-secondary); text-transform: uppercase; display: block;">Custo Estimado</span>
						<strong id="stat-total-cost" style="font-size: 1.8rem; font-weight: 900; color: #1e40af; line-height: 1.1;">R$ 550 DIY</strong>
						<span id="stat-cost-sub" style="font-size: 0.75rem; color: #64748b; display: block; margin-top: 2px;">(Economiza R$ 1.800 vs Barista)</span>
					</div>
				</div>

				<div style="background: #f0fdf4; border: 1.5px solid #16a34a; border-radius: 6px; padding: 0.85rem; margin-bottom: 1.25rem; font-size: 0.85rem;">
					<strong style="color: #166534; display: block; margin-bottom: 0.35rem;">🥛 Leites & Adoçantes:</strong>
					<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.5rem; color: #14532d;">
						<div>• <strong id="stat-whole-milk">8 Litros</strong> Leite Integral / Sem Lactose</div>
						<div>• <strong id="stat-oat-milk">6 Litros</strong> Leite de Aveia Barista</div>
						<div>• <strong id="stat-sugar-lbs">1,5 kg</strong> Açúcar Demerara e Adoçante</div>
						<div>• <strong id="stat-syrup-bottles">3 Garrafas (750ml)</strong> Xaropes</div>
					</div>
				</div>

				<div style="background: #f8fafc; border: 1.5px solid var(--fg-pencil); border-radius: 6px; padding: 0.85rem; margin-bottom: 1.25rem; font-size: 0.88rem;">
					<div style="display: flex; justify-content: space-between; padding: 0.25rem 0; border-bottom: 1px dashed #cbd5e1;">
						<span style="color: #475569;">🫖 Garrafas / Cafeteiras:</span>
						<strong id="stat-urn-req" style="color: var(--fg-pencil);">2 Garrafas Térmicas Inox de 8L</strong>
					</div>
					<div style="display: flex; justify-content: space-between; padding: 0.25rem 0; border-bottom: 1px dashed #cbd5e1;">
						<span style="color: #475569;">🥤 Copos e Tampas:</span>
						<strong id="stat-cups-count" style="color: var(--fg-pencil);">155 Sets (+15% margem)</strong>
					</div>
					<div style="display: flex; justify-content: space-between; padding: 0.25rem 0;">
						<span style="color: #475569;">🥢 Mexedores e Guardanapos:</span>
						<strong id="stat-stirrers-count" style="color: #15803d;">200 Mexedores de madeira e guardanapos</strong>
					</div>
				</div>

				<div style="display: flex; flex-wrap: wrap; gap: 0.5rem;">
					<button type="button" id="btn-print-summary" class="hand-btn" style="flex: 1; background: var(--accent-red); color: #fff; padding: 0.6rem 1rem; font-size: 0.9rem; font-weight: 700; border: 2px solid var(--fg-pencil); border-radius: 6px; cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 0.4rem; box-shadow: 2px 2px 0px var(--fg-pencil);">
						<span>🖨️</span> Imprimir Lista de Compras (PDF)
					</button>
					<button type="button" id="btn-export-doc" class="hand-btn" style="flex: 1; background: #fff; color: var(--fg-pencil); padding: 0.6rem 1rem; font-size: 0.9rem; font-weight: 700; border: 2px solid var(--fg-pencil); border-radius: 6px; cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 0.4rem; box-shadow: 2px 2px 0px var(--fg-pencil);">
						<span>📄</span> Exportar Word (.doc)
					</button>
				</div>
			</div>

		</div>

		<!-- Related Tools Component -->
		<RelatedTools currentPath="/pt/wedding-coffee-bar-calculator" category="dining" />

	</main>

	<!-- Hidden Print Containers for Vector PDF / Window.print -->
	<div id="print-root" class="print-only">
		<div id="print-coffee-container" class="print-page" style="padding: 10mm;">
			<div style="border-bottom: 2pt solid #000; padding-bottom: 4mm; margin-bottom: 6mm;">
				<h1 style="font-size: 20pt; margin: 0 0 3mm 0; color: #000;">Lista de Compras Coffee Bar de Casamento</h1>
				<p id="print-coffee-meta" style="font-size: 10.5pt; color: #333; margin: 0;">Café em Grãos, Leites, Xaropes & Garrafas Térmicas | Manage.Wedding</p>
			</div>
			<div id="print-coffee-tables">
			</div>
		</div>
	</div>
</Layout>

<style>
	.hand-card {
		background: #fff;
		border: 2px solid var(--fg-pencil);
		border-radius: 8px;
		box-shadow: 4px 4px 0px var(--fg-pencil);
	}

	.badge-postit {
		background: var(--post-it);
		border: 1.5px solid var(--fg-pencil);
		box-shadow: 2px 2px 0px var(--fg-pencil);
	}
</style>

<style is:global>
	.print-only {
		display: none;
	}

	@media print {
		nav,
		footer,
		.nav-sketch,
		.site-footer,
		.site-header,
		.nav-links-desktop,
		.lang-dropdown-wrapper,
		.hamburger-btn,
		.no-print,
		main,
		.breadcrumb-nav {
			display: none !important;
		}

		.print-only,
		#print-root {
			display: block !important;
			visibility: visible !important;
			position: static !important;
			width: 100% !important;
			background: #fff !important;
			color: #000 !important;
		}

		#print-root * {
			visibility: visible !important;
		}

		.print-page {
			page-break-after: always !important;
			break-after: page !important;
		}
	}
</style>

<script is:inline>
	const inputCoffeeGuests = document.getElementById('input-coffee-guests');
	const selectCoffeeSetup = document.getElementById('select-coffee-setup');
	const selectCoffeeTiming = document.getElementById('select-coffee-timing');
	const selectCoffeeSeason = document.getElementById('select-coffee-season');
	const selectSyrupTier = document.getElementById('select-syrup-tier');
	const selectCupType = document.getElementById('select-cup-type');

	const badgeTotalCups = document.getElementById('badge-total-cups');
	const statCoffeeLbs = document.getElementById('stat-coffee-lbs');
	const statCoffeeSub = document.getElementById('stat-coffee-sub');
	const statTotalCost = document.getElementById('stat-total-cost');
	const statCostSub = document.getElementById('stat-cost-sub');

	const statWholeMilk = document.getElementById('stat-whole-milk');
	const statOatMilk = document.getElementById('stat-oat-milk');
	const statSugarLbs = document.getElementById('stat-sugar-lbs');
	const statSyrupBottles = document.getElementById('stat-syrup-bottles');

	const statUrnReq = document.getElementById('stat-urn-req');
	const statCupsCount = document.getElementById('stat-cups-count');
	const statStirrersCount = document.getElementById('stat-stirrers-count');

	function calculateCoffee() {
		const guests = Math.max(10, parseInt(inputCoffeeGuests.value, 10) || 130);
		const setup = selectCoffeeSetup.value;
		const timing = selectCoffeeTiming.value;
		const season = selectCoffeeSeason.value;

		let drinkRate = 0.70;
		if (season === 'spring-summer') drinkRate = 0.55;
		if (timing === 'brunch') drinkRate = 0.85;

		const coffeeDrinkers = Math.round(guests * drinkRate);
		const avgCupsPerPerson = timing === 'brunch' ? 1.7 : 1.4;
		const totalCups = Math.round(coffeeDrinkers * avgCupsPerPerson);

		badgeTotalCups.textContent = `${totalCups} Xícaras de Café`;

		let regRatio = 0.70;
		let decafRatio = 0.30;
		if (timing === 'late-night') {
			regRatio = 0.85;
			decafRatio = 0.15;
		} else if (timing === 'brunch') {
			regRatio = 0.90;
			decafRatio = 0.10;
		}

		const totalKg = (totalCups / 65).toFixed(1);
		const regKg = (totalKg * regRatio).toFixed(1);
		const decafKg = (totalKg * decafRatio).toFixed(1);

		statCoffeeLbs.textContent = `${totalKg} kg Grãos`;
		statCoffeeSub.textContent = `(${regKg} kg Tradicional / ${decafKg} kg Descafeinado)`;

		const totalLitersMilk = Math.round(totalCups * 0.09);
		const wholeMilkLit = Math.round(totalLitersMilk * 0.60);
		const oatMilkLit = Math.round(totalLitersMilk * 0.40);

		statWholeMilk.textContent = `${Math.max(4, wholeMilkLit)} Litros Leite Integral / Sem Lactose`;
		statOatMilk.textContent = `${Math.max(3, oatMilkLit)} Litros Leite de Aveia Barista`;

		const sugarWeight = Math.max(1, (totalCups * 0.012).toFixed(1));
		statSugarLbs.textContent = `${sugarWeight} kg Açúcar e Adoçante`;

		const syrupTier = selectSyrupTier.value;
		let syrupText = '3 Garrafas (750ml)';
		if (syrupTier === 'deluxe') syrupText = '5 Garrafas (750ml) Deluxe';
		else if (syrupTier === 'none') syrupText = '0 (Apenas açúcar e leite)';
		statSyrupBottles.textContent = syrupText;

		const cupsTotal = Math.ceil(totalCups * 1.15);
		statCupsCount.textContent = `${cupsTotal} Sets (${selectCupType.options[selectCupType.selectedIndex].text})`;
		statStirrersCount.textContent = `${cupsTotal + 50} Mexedores de madeira e guardanapos`;

		if (setup === 'espresso-cart') {
			statUrnReq.textContent = 'Máquina de Espresso Profissional 2 Grupos';
		} else if (totalCups <= 80) {
			statUrnReq.textContent = '2 Garrafas Térmicas de 5L (1 Trad. / 1 Desc.)';
		} else {
			statUrnReq.textContent = '2 Garrafas Térmicas Inox de 8L';
		}

		let cost = 550;

		if (setup === 'diy-urn') {
			cost = Math.round((totalKg * 85) + (wholeMilkLit * 6) + (oatMilkLit * 15) + 120 + 80);
			statTotalCost.textContent = `R$ ${cost} DIY`;
			const savings = Math.max(800, 2400 - cost);
			statCostSub.textContent = `(Economiza ~R$ ${savings} vs Barista)`;
		} else if (setup === 'espresso-cart') {
			cost = 2400 + (guests > 150 ? 500 : 0);
			statTotalCost.textContent = `R$ ${cost} Barista`;
			statCostSub.textContent = 'Inclui Barista & Latte Art';
		} else if (setup === 'drip-caterer') {
			cost = Math.round(guests * 9.50);
			statTotalCost.textContent = `R$ ${cost} Buffet`;
			statCostSub.textContent = 'Serviço servido com a sobremesa';
		} else {
			cost = Math.round(450 + (totalCups * 2.50));
			statTotalCost.textContent = `R$ ${cost} Cold Brew`;
			statCostSub.textContent = 'Barril gelado e espuma doce';
		}

		document.getElementById('print-coffee-tables').innerHTML = `
			<table style="width:100%; border-collapse:collapse; font-size:10pt; text-align:left; margin-top:10px;">
				<thead>
					<tr style="background:#e2e8f0; border-bottom:2pt solid #000;">
						<th style="padding:6pt;">Item / Insumo</th>
						<th style="padding:6pt;">Especificação</th>
						<th style="padding:6pt; text-align:right;">Quantidade Necessária</th>
					</tr>
				</thead>
				<tbody>
					<tr style="border-bottom:1pt solid #cbd5e1;">
						<td style="padding:6pt; font-weight:bold;">Café Especial em Grãos</td>
						<td style="padding:6pt;">${regKg} kg Tradicional + ${decafKg} kg Descafeinado</td>
						<td style="padding:6pt; text-align:right; font-weight:bold; color:#b91c1c;">${totalKg} kg Grãos</td>
					</tr>
					<tr style="border-bottom:1pt solid #cbd5e1;">
						<td style="padding:6pt; font-weight:bold;">Leites e Bebidas Vegetais</td>
						<td style="padding:6pt;">60% Leite integral, 40% Bebida de aveia barista</td>
						<td style="padding:6pt; text-align:right;">${statWholeMilk.textContent} + ${statOatMilk.textContent}</td>
					</tr>
					<tr style="border-bottom:1pt solid #cbd5e1;">
						<td style="padding:6pt; font-weight:bold;">Xaropes e Adoçantes</td>
						<td style="padding:6pt;">${syrupText}, açúcar demerara e sachês de adoçante</td>
						<td style="padding:6pt; text-align:right;">${statSugarLbs.textContent}</td>
					</tr>
					<tr style="border-bottom:1pt solid #cbd5e1;">
						<td style="padding:6pt; font-weight:bold;">Utensílios e Copos</td>
						<td style="padding:6pt;">${statUrnReq.textContent}</td>
						<td style="padding:6pt; text-align:right;"><strong>${statCupsCount.textContent}</strong></td>
					</tr>
					<tr style="background:#eff6ff; font-weight:bold; font-size:11pt; border-top:2pt solid #000;">
						<td style="padding:8pt;">VALOR TOTAL ESTIMADO</td>
						<td style="padding:8pt;">${selectCoffeeSetup.options[selectCoffeeSetup.selectedIndex].text}</td>
						<td style="padding:8pt; text-align:right; color:#1e40af;">${statTotalCost.textContent}</td>
					</tr>
				</tbody>
			</table>
		`;
	}

	[inputCoffeeGuests, selectCoffeeSetup, selectCoffeeTiming, selectCoffeeSeason, selectSyrupTier, selectCupType].forEach(el => {
		el.addEventListener('input', calculateCoffee);
		el.addEventListener('change', calculateCoffee);
	});

	document.getElementById('btn-print-summary').addEventListener('click', () => {
		calculateCoffee();
		window.print();
	});

	document.getElementById('btn-export-doc').addEventListener('click', () => {
		calculateCoffee();
		const coffee = statCoffeeLbs.textContent;
		const cost = statTotalCost.textContent;
		const cups = badgeTotalCups.textContent;
		const guests = inputCoffeeGuests.value;

		const html = `
			<html xmlns:o='urn:schemas-microsoft-com:office:office' xmlns:w='urn:schemas-microsoft-com:office:word' xmlns='http://www.w3.org/TR/REC-html40'>
			<head><meta charset='utf-8'><title>Insumos Coffee Bar</title>
			<style>body{font-family:Arial,sans-serif;line-height:1.4;} table{border-collapse:collapse;width:100%;} th,td{padding:6pt;border:1pt solid #ccc;text-align:left;}</style>
			</head>
			<body>
				<h1>Lista de Compras Coffee Bar de Casamento</h1>
				<p><strong>Convidados:</strong> ${guests} | <strong>Xícaras:</strong> ${cups} | <strong>Grãos:</strong> ${coffee} | <strong>Orçamento:</strong> ${cost}</p>
				<hr/>
				<p><em>Gerado por Manage.Wedding</em></p>
			</body>
			</html>
		`;

		const blob = new Blob(['\ufeff', html], { type: 'application/msword' });
		const url = URL.createObjectURL(blob);
		const a = document.createElement('a');
		a.href = url;
		a.download = `Coffee-Bar-Casamento-${new Date().toISOString().slice(0,10)}.doc`;
		document.body.appendChild(a);
		a.click();
		document.body.removeChild(a);
		URL.revokeObjectURL(url);
	});

	calculateCoffee();
</script>
