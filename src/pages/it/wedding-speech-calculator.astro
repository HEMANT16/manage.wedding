---
import Layout from '../../layouts/Layout.astro';
import RelatedTools from '../../components/RelatedTools.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';

const schema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": ["WebApplication", "CalculatorApplication"],
      "@id": "https://manage.wedding/it/wedding-speech-calculator/#webapp",
      "name": "Calcolatore Durata Discorsi di Matrimonio & Schede Testimoni",
      "url": "https://manage.wedding/it/wedding-speech-calculator",
      "inLanguage": "it",
      "description": "Calcola la durata esatta e il numero di parole per i discorsi di nozze (testimone dello sposo, damigella d'onore, genitori e brindisi degli sposi). Cronometro di prova e schede promemoria stampabili in PDF.",
      "applicationCategory": "UtilitiesApplication",
      "operatingSystem": "All",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "EUR"
      }
    },
    {
      "@type": "FAQPage",
      "inLanguage": "it",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "Quanto dovrebbe durare un discorso di matrimonio?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "La durata ideale per un discorso di matrimonio è tra 3 e 5 minuti (circa 400–650 parole). I discorsi inferiori a 2 minuti risultano frettolosi, mentre quelli oltre i 6 minuti rischiano di annoiare gli invitati."
          }
        },
        {
          "@type": "Question",
          "name": "Quante parole al minuto si pronunciano durante un brindisi di nozze?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Il ritmo medio naturale di lettura in pubblico è di circa 125–130 parole al minuto (WPM), considerando pause naturali, risate del pubblico e sguardi verso gli sposi."
          }
        }
      ]
    },
    {
      "@type": "HowTo",
      "name": "Come calcolare e provare il discorso di matrimonio",
      "description": "Metodo pratico per scrivere, calcolare i minuti, provare a voce alta e stampare le schede per il leggio.",
      "inLanguage": "it",
      "step": [
        {
          "@type": "HowToStep",
          "name": "1. Scegliere il modello di discorso o incollare il testo",
          "text": "Seleziona il modello per testimone, damigella, genitori o sposi, oppure incolla la tua bozza."
        },
        {
          "@type": "HowToStep",
          "name": "2. Puntare a 3-5 minuti (400–650 parole)",
          "text": "Mantieni la lunghezza nel range ideale per preservare l'attenzione e l'emozione della sala."
        },
        {
          "@type": "HowToStep",
          "name": "3. Provare a voce alta con il cronometro",
          "text": "Usa il cronometro integrato per testare il ritmo di respirazione e le pause per le risate."
        },
        {
          "@type": "HowToStep",
          "name": "4. Stampare le schede promemoria",
          "text": "Esporta le schede in formato PDF o Word a caratteri grandi per leggerle agevolmente durante il banchetto."
        }
      ]
    }
  ]
};
---

<Layout 
	title="Calcolatore Durata Discorsi di Matrimonio & Schede Testimoni | Manage.Wedding"
	description="Calcola la durata esatta e il numero di parole per i discorsi di nozze (testimone dello sposo, damigella d'onore, genitori e brindisi degli sposi). Cronometro di prova e schede promemoria stampabili in PDF."
	schema={schema}
>
	<main style="max-width: 1200px; margin: 0 auto; padding: clamp(0.75rem, 2.5vw, 2rem); box-sizing: border-box; overflow-x: hidden;">
		<Breadcrumb title="Calcolatore Discorsi & Brindisi" category="stationery" />
		
		<!-- Hero & Header -->
		<section class="section-paper" style="margin-bottom: 1.5rem; text-align: center;">
			<div style="max-width: 900px; margin: 0 auto; padding: 2.5rem 1rem;">
				<div class="tape" style="margin: 0 auto 1rem;"></div>
				<h1 class="font-heading" style="font-size: clamp(32px, 5vw, 56px); color: var(--fg-pencil); line-height: 1.15; text-align: center; margin-bottom: 0.75rem;">
					Calcolatore Discorsi di Matrimonio
				</h1>
				<p class="font-body" style="font-size: 1.25rem; color: var(--fg-pencil); max-width: 680px; margin: 0 auto; text-align: center;">
					Calcola il minutaggio esatto, imposta la velocità di lettura, prova con il cronometro live e stampa le <strong>Schede Promemoria per il Leggio</strong> in Word o PDF. 100% privato nel tuo browser!
				</p>
			</div>
		</section>

		<!-- 3-Step Visual Quick Guide Card -->
		<div class="hand-card" style="margin-bottom: 2rem; padding: 1.25rem; background: var(--bg-paper); border: 2px solid var(--fg-pencil);">
			<h2 class="font-heading" style="font-size: 1.25rem; color: var(--fg-pencil); margin: 0 0 0.75rem 0; text-align: center; display: flex; align-items: center; justify-content: center; gap: 0.5rem;">
				<span>📖</span> Come Calcolare e Perfezionare il Discorso in 3 Passaggi
			</h2>
			<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1rem;">
				<div style="background: #fff; border: 1.5px solid var(--fg-pencil); border-radius: 4px; padding: 0.85rem;">
					<strong class="font-heading" style="display: block; color: var(--accent-red); font-size: 1rem; margin-bottom: 0.25rem;">
						1️⃣ Incolla il Testo o Carica un Modello
					</strong>
					<p class="font-body" style="font-size: 0.85rem; margin: 0; line-height: 1.4; color: var(--fg-pencil);">
						Incolla la tua bozza o clicca su uno dei modelli per Testimone, Damigella, Genitori o Ringraziamento Sposi.
					</p>
				</div>

				<div style="background: #fff; border: 1.5px solid var(--fg-pencil); border-radius: 4px; padding: 0.85rem;">
					<strong class="font-heading" style="display: block; color: var(--accent-blue); font-size: 1rem; margin-bottom: 0.25rem;">
						2️⃣ Imposta il Ritmo e Prova con il Cronometro
					</strong>
					<p class="font-body" style="font-size: 0.85rem; margin: 0; line-height: 1.4; color: var(--fg-pencil);">
						Scegli la velocità (110, 130 o 155 parole al minuto) e leggi ad alta voce con il timer interattivo.
					</p>
				</div>

				<div style="background: #fff; border: 1.5px solid var(--fg-pencil); border-radius: 4px; padding: 0.85rem;">
					<strong class="font-heading" style="display: block; color: #16a34a; font-size: 1rem; margin-bottom: 0.25rem;">
						3️⃣ Esporta le Schede Stampabili per il Microfono
					</strong>
					<p class="font-body" style="font-size: 0.85rem; margin: 0; line-height: 1.4; color: var(--fg-pencil);">
						Clicca su <strong>"🖨️ Stampa Schede PDF"</strong> per generare cartoncini a caratteri grandi (16pt) con pause segnate.
					</p>
				</div>
			</div>
		</div>

		<!-- Main Workspace Grid -->
		<div style="display: grid; grid-template-columns: 1fr; lg:grid-template-columns: 1.15fr 0.85fr; gap: 2rem; align-items: start; margin-bottom: 3.5rem;" class="speech-calc-grid">
			
			<!-- Left Column: Speech Text Editor & Preset Loader -->
			<div class="hand-card" style="padding: 1.75rem; background: #fff;">
				
				<div style="margin-bottom: 1.25rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.75rem;">
					<div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.4rem;">
						<div>
							<h2 class="font-heading" style="font-size: 1.4rem; color: var(--fg-pencil); margin: 0;">
								📝 Editor Testo & Brindisi
							</h2>
							<span class="font-body" style="font-size: 0.85rem; color: var(--text-secondary); display: block;">
								Analisi 100% privata nel browser
							</span>
						</div>
						<button type="button" id="btn-clear-speech" class="hand-btn secondary" style="padding: 0.25rem 0.55rem; font-size: 0.75rem; color: #b91c1c;">
							🗑️ Cancella Testo
						</button>
					</div>
				</div>

				<!-- Quick Template Pills -->
				<div style="background: var(--bg-paper); border: 1.5px dashed var(--fg-pencil); border-radius: 4px; padding: 0.65rem; margin-bottom: 1rem;">
					<strong class="font-heading" style="font-size: 0.85rem; color: var(--fg-pencil); display: block; margin-bottom: 0.35rem;">
						⚡ Modelli Strutturati per i Discorsi:
					</strong>
					<div style="display: flex; flex-wrap: wrap; gap: 0.35rem;">
						<button type="button" class="btn-load-template hand-btn" data-type="bestman" style="padding: 0.2rem 0.5rem; font-size: 0.75rem; background: #eff6ff;">
							🤵 Testimone dello Sposo
						</button>
						<button type="button" class="btn-load-template hand-btn" data-type="moh" style="padding: 0.2rem 0.5rem; font-size: 0.75rem; background: #fdf2f8;">
							💐 Damigella / Testimone Sposa
						</button>
						<button type="button" class="btn-load-template hand-btn" data-type="parent" style="padding: 0.2rem 0.5rem; font-size: 0.75rem; background: #f0fdf4;">
							🥂 Discorso dei Genitori
						</button>
						<button type="button" class="btn-load-template hand-btn" data-type="couple" style="padding: 0.2rem 0.5rem; font-size: 0.75rem; background: #fefce8;">
							💍 Ringraziamento degli Sposi
						</button>
					</div>
				</div>

				<!-- Speech Textarea -->
				<div style="margin-bottom: 1rem;">
					<label for="speech-editor" class="font-heading" style="display: block; font-size: 0.9rem; color: var(--fg-pencil); margin-bottom: 0.3rem;">
						Scrivi o Incolla il Testo del Discorso:
					</label>
					<textarea id="speech-editor" rows="14" placeholder="Inizia a scrivere il tuo discorso o brindisi qui..." style="width: 100%; box-sizing: border-box; padding: 0.85rem; border: 2px solid var(--fg-pencil); border-radius: 4px; font-family: var(--font-body); font-size: 1rem; line-height: 1.6; background: #faf8f5; resize: vertical;"></textarea>
				</div>

				<!-- Pacing & Delivery Controls -->
				<div style="display: grid; grid-template-columns: 1.2fr 1fr; gap: 0.75rem; background: var(--bg-paper); border: 1.5px solid var(--fg-pencil); border-radius: 4px; padding: 0.75rem;">
					<div>
						<label for="select-speaking-pace" class="font-heading" style="display: block; font-size: 0.85rem; color: var(--accent-red); margin-bottom: 0.2rem;">
							Velocità di Lettura
						</label>
						<select id="select-speaking-pace" style="width: 100%; box-sizing: border-box; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; background: #fff; font-size: 0.85rem;">
							<option value="110">Lento ed Emozionale (110 parole/min)</option>
							<option value="130" selected>Conversazionale Naturale (130 parole/min)</option>
							<option value="155">Veloce ed Energico (155 parole/min)</option>
						</select>
					</div>

					<div>
						<label for="select-pause-buffer" class="font-heading" style="display: block; font-size: 0.85rem; color: var(--accent-blue); margin-bottom: 0.2rem;">
							Cuscinetto Risate & Applausi
						</label>
						<select id="select-pause-buffer" style="width: 100%; box-sizing: border-box; padding: 0.4rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px; background: #fff; font-size: 0.85rem;">
							<option value="0">Nessuno (Lettura continua)</option>
							<option value="30" selected>+30s (Risate e brindisi moderati)</option>
							<option value="60">+60s (Molte battute ed applausi)</option>
						</select>
					</div>
				</div>

			</div>

			<!-- Right Column: Live Time Dashboard -->
			<div style="display: flex; flex-direction: column; gap: 1.5rem;">
				
				<!-- Live Timing Dashboard Card -->
				<div class="hand-card" style="padding: 1.75rem; background: #fff;">
					
					<div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 1.25rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.75rem;">
						<div style="width: 100%;">
							<h3 class="font-heading" style="font-size: 1.4rem; color: var(--fg-pencil); margin: 0; text-align: center;">
								⏱️ Durata Stimata del Discorso
							</h3>
							<span class="font-body" style="font-size: 0.85rem; color: var(--text-secondary); display: block; text-align: center;">
								Calcolata a <span id="display-wpm-rate">130</span> parole al minuto
							</span>
						</div>

						<!-- Exports Toolbar -->
						<div style="display: flex; gap: 0.4rem; flex-wrap: wrap; justify-content: center; width: 100%; margin-top: 0.5rem;">
							<button type="button" id="btn-export-doc" class="hand-btn" style="padding: 0.35rem 0.75rem; font-size: 0.85rem; background: #eff6ff;">
								📝 Esporta Word (.doc)
							</button>
							<button type="button" id="btn-export-pdf" class="hand-btn" style="padding: 0.35rem 0.75rem; font-size: 0.85rem; background: #fef2f2;">
								🖨️ Stampa Schede PDF
							</button>
							<button type="button" id="btn-export-csv" class="hand-btn secondary" style="padding: 0.35rem 0.75rem; font-size: 0.85rem;">
								📊 CSV
							</button>
						</div>
					</div>

					<!-- Big Estimated Time Display -->
					<div style="background: #f0fdf4; border: 2.5px solid #16a34a; border-radius: 6px; padding: 1.2rem; text-align: center; margin-bottom: 1.25rem;">
						<span style="font-size: 0.88rem; color: #16a34a; font-weight: bold; text-transform: uppercase; letter-spacing: 1px;">
							Tempo di Lettura Stimato
						</span>
						<strong id="display-time-result" class="font-heading" style="font-size: 2.8rem; color: #166534; display: block; line-height: 1.15; margin: 0.2rem 0;">
							3m 45s
						</strong>
						<span id="display-speech-zone" style="font-size: 0.85rem; font-weight: bold; color: #15803d;">
							✅ Durata Ideale (3–5 minuti)
						</span>
					</div>

					<!-- Detailed Metric Numbers -->
					<div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 0.5rem; text-align: center; margin-bottom: 1.25rem;">
						<div style="border: 1px solid var(--fg-pencil); border-radius: 4px; padding: 0.5rem; background: var(--bg-paper);">
							<span style="font-size: 0.75rem; color: var(--text-secondary); display: block;">Parole Totali</span>
							<strong id="display-word-count" style="font-size: 1.2rem; color: var(--fg-pencil);">485</strong>
						</div>

						<div style="border: 1px solid var(--fg-pencil); border-radius: 4px; padding: 0.5rem; background: var(--bg-paper);">
							<span style="font-size: 0.75rem; color: var(--text-secondary); display: block;">Paragrafi</span>
							<strong id="display-paragraph-count" style="font-size: 1.2rem; color: var(--fg-pencil);">5</strong>
						</div>

						<div style="border: 1px solid var(--fg-pencil); border-radius: 4px; padding: 0.5rem; background: var(--bg-paper);">
							<span style="font-size: 0.75rem; color: var(--text-secondary); display: block;">Cartoncini</span>
							<strong id="display-cards-count" style="font-size: 1.2rem; color: var(--fg-pencil);">3 Schede</strong>
						</div>
					</div>

					<!-- Speech Length Progress Bar -->
					<div>
						<div style="display: flex; justify-content: space-between; font-size: 0.78rem; font-weight: bold; margin-bottom: 0.25rem;">
							<span>0 min</span>
							<span style="color: #16a34a;">3 - 5 min (Ideale)</span>
							<span style="color: var(--accent-red);">7+ min (Troppo lungo)</span>
						</div>
						<div style="width: 100%; height: 18px; border: 1.5px solid var(--fg-pencil); border-radius: 4px; overflow: hidden; background: #eee; position: relative;">
							<div id="speech-gauge-bar" style="width: 50%; height: 100%; background: #16a34a; transition: width 0.3s ease, background-color 0.3s ease;"></div>
						</div>
					</div>

				</div>

				<!-- Live Rehearsal Stopwatch Card -->
				<div class="hand-card" style="padding: 1.25rem; background: var(--bg-paper); border: 2px solid var(--fg-pencil);">
					<strong class="font-heading" style="font-size: 1.05rem; color: var(--fg-pencil); display: block; margin-bottom: 0.3rem; text-align: center;">
						🎙️ Cronometro di Prova a Voce Alta
					</strong>
					<p style="font-size: 0.8rem; color: var(--text-secondary); margin: 0 0 0.75rem 0; text-align: center;">
						Leggi il discorso a voce alta per verificare la tua velocità naturale:
					</p>

					<div style="display: flex; justify-content: center; align-items: center; gap: 0.75rem; margin-bottom: 0.75rem;">
						<span id="stopwatch-display" style="font-family: monospace; font-size: 2rem; font-weight: bold; color: var(--fg-pencil); background: #fff; padding: 0.3rem 0.8rem; border: 1.5px solid var(--fg-pencil); border-radius: 4px;">
							00:00.0
						</span>
					</div>

					<div style="display: flex; justify-content: center; gap: 0.4rem;">
						<button type="button" id="btn-timer-start" class="hand-btn" style="padding: 0.35rem 0.85rem; font-size: 0.85rem; background: #dcfce7; color: #166534;">
							▶️ Avvia Prova
						</button>
						<button type="button" id="btn-timer-pause" class="hand-btn" style="padding: 0.35rem 0.85rem; font-size: 0.85rem; background: #fefce8; color: #854d0e; display: none;">
							⏸️ Pausa
						</button>
						<button type="button" id="btn-timer-reset" class="hand-btn secondary" style="padding: 0.35rem 0.85rem; font-size: 0.85rem;">
							🔄 Reimposta
						</button>
					</div>
				</div>

			</div>

		</div>

		<!-- Hidden Printable Sheet for PDF Generation (Cue Cards) -->
		<div id="printable-pdf-sheet" class="print-only">
			<div style="border-bottom: 2px solid #000; padding-bottom: 8px; margin-bottom: 20px; text-align: center;">
				<h1 style="font-size: 22px; margin: 0 0 4px 0;">Schede Promemoria Discorso di Matrimonio</h1>
				<p style="margin: 0; font-size: 12px; color: #555;">Durata Stimata: <span id="print-est-time"></span> | Parole: <span id="print-word-count"></span></p>
			</div>
			<div id="print-cue-cards-grid" style="display: flex; flex-direction: column; gap: 20px;"></div>
		</div>

		<!-- SEO Article -->
		<article class="hand-card" style="padding: clamp(1.5rem, 4vw, 3.5rem); margin-bottom: 4rem;">
			<div style="text-align: center; max-width: 800px; margin: 0 auto 2.5rem;">
				<h2 class="font-heading" style="font-size: clamp(2rem, 4vw, 2.8rem); color: var(--fg-pencil); margin-bottom: 0.75rem;">
					Guida Completa ai Discorsi di Matrimonio & Brindisi
				</h2>
				<p class="font-body" style="font-size: 1.15rem; color: var(--text-secondary); line-height: 1.6;">
					La regola dei 3–5 minuti, il controllo delle emozioni e perché non leggere mai dallo smartphone.
				</p>
			</div>

			<section style="margin-bottom: 3rem;">
				<h3 class="font-heading" style="font-size: 1.75rem; color: var(--accent-red); margin-bottom: 1rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.5rem;">
					1. La Regola Aurea dei 3–5 Minuti
				</h3>
				<p class="font-body" style="font-size: 1.05rem; line-height: 1.7; color: var(--fg-pencil); margin-bottom: 1rem;">
					Durante il banchetto nuziale l'attenzione della sala è preziosa. Un discorso di 400–600 parole pronunciato con calma (circa 130 parole al minuto) lascia il segno senza rischiare che i piatti si raffreddino.
				</p>
			</section>
		</article>

		<RelatedTools currentPath="/it/wedding-speech-calculator" category="stationery" />
	</main>
</Layout>

<style>
	.print-only {
		display: none;
	}

	
</style>

<script is:inline>
	const sampleTemplates = {
		bestman: `Buonasera a tutti! Per chi non mi conosce, sono Marco, il migliore amico e testimone dello sposo dai tempi dell'università.

Quando ho conosciuto Alessandro, mi disse che i suoi tre grandi sogni erano diventare un architetto di successo, imparare a cucinare qualcosa di diverso dalla pasta al tonno e trovare una donna intelligente, bellissima e con la pazienza di sopportare le sue domeniche di calcio. Beh, guardando la splendida sposa stasera, direi che ha fatto centro su tutto! [Pausa per le risate]

Scherzi a parte, vedere l'amore e la complicità nei vostri occhi è una vera ispirazione. Chiara tira fuori il meglio di lui con la sua dolcezza e il suo sorriso. Siete davvero una coppia straordinaria.

Alziamo tutti i calici: a una vita intera di felicità, risate e amore incondizionato. Agli sposi! Viva gli sposi! [Tutti brindano]`,

		moh: `Ciao a tutti! Sono Sara, la testimone e sorella della sposa. Guardando Chiara stasera, sono emozionatissima per quanto sia radiosa e felice.

Siamo cresciute condividendo tutto: vestiti, segreti notturni e sogni su come sarebbe stato il nostro matrimonio. Quando mi ha parlato di Alessandro dopo i primi appuntamenti, non mi ha detto semplicemente di essersi divertita, ma mi ha detto con gli occhi lucidi: 'Sara, ho incontrato l'uomo della mia vita.' [Pausa per i sorrisi]

Ad Alessandro: grazie per come ami e rispetti mia sorella ogni singolo giorno. Vedervi così felici è il regalo più bello che la nostra famiglia potesse desiderare.

Un brindisi agli sposi: che la vostra vita insieme sia colma di gioia e meraviglia! Viva gli sposi!`,

		parent: `Benvenuti a tutti! A nome delle nostre famiglie, vogliamo ringraziare ciascuno di voi per essere qui oggi a festeggiare questo momento indimenticabile.

Vedere nostra figlia crescere e diventare la donna straordinaria e generosa che è oggi è stato l'onore più grande della nostra vita. E accogliere Alessandro nella nostra famiglia è una gioia immensa.

Il matrimonio è la scelta quotidiana di amarsi, perdonarsi, sostenersi e ridere insieme in ogni momento della vita. Ricordatevi sempre delle promesse che vi siete scambiati oggi.

Alziamo i calici alla vostra salute, all'amore e al vostro splendido futuro insieme! Auguri!`,

		couple: `Vogliamo rubarvi solo un minuto per ringraziare di cuore ognuno di voi. Avere qui riunite tutte le persone più importanti della nostra vita è un'emozione indescrivibile.

Ai nostri genitori: grazie per il vostro amore incondizionato, per i sacrifici e per l'esempio meraviglioso che ci avete dato ogni giorno.

Ai nostri testimoni, agli amici e a tutti i fornitori: grazie per aver reso magica questa giornata.

Ora basta discorsi: l'open bar è aperto, i musicisti sono pronti, scateniamoci in pista! Viva l'amore!`
	};

	const speechEditor = document.getElementById('speech-editor');
	const paceSelect = document.getElementById('select-speaking-pace');
	const bufferSelect = document.getElementById('select-pause-buffer');
	const displayWpmRate = document.getElementById('display-wpm-rate');

	const displayTimeResult = document.getElementById('display-time-result');
	const displaySpeechZone = document.getElementById('display-speech-zone');
	const displayWordCount = document.getElementById('display-word-count');
	const displayParagraphCount = document.getElementById('display-paragraph-count');
	const displayCardsCount = document.getElementById('display-cards-count');
	const speechGaugeBar = document.getElementById('speech-gauge-bar');

	function calculateStats() {
		const text = speechEditor.value.trim();
		const wpm = parseInt(paceSelect.value) || 130;
		const bufferSec = parseInt(bufferSelect.value) || 30;

		displayWpmRate.textContent = wpm;

		let words = 0;
		let paragraphs = 0;

		if (text.length > 0) {
			words = text.split(/\s+/).filter(w => w.length > 0).length;
			paragraphs = text.split(/\n+/).filter(p => p.trim().length > 0).length;
		}

		displayWordCount.textContent = words;
		displayParagraphCount.textContent = paragraphs;

		const estimatedCards = Math.max(1, Math.ceil(words / 150));
		displayCardsCount.textContent = `${estimatedCards} Schede`;

		let totalSeconds = 0;
		if (words > 0) {
			totalSeconds = Math.round((words / wpm) * 60) + bufferSec;
		}

		const mins = Math.floor(totalSeconds / 60);
		const secs = totalSeconds % 60;
		displayTimeResult.textContent = `${mins}m ${secs}s`;

		const totalMins = totalSeconds / 60;
		const pct = Math.min(100, Math.max(5, (totalMins / 8) * 100));
		speechGaugeBar.style.width = `${words === 0 ? 0 : pct}%`;

		if (words === 0) {
			displaySpeechZone.textContent = "Inizia a scrivere o carica un modello sopra";
			displaySpeechZone.style.color = "var(--text-secondary)";
			speechGaugeBar.style.backgroundColor = "#ccc";
		} else if (totalMins < 2.5) {
			displaySpeechZone.textContent = "⚡ Breve (Puoi aggiungere un aneddoto)";
			displaySpeechZone.style.color = "var(--accent-blue)";
			speechGaugeBar.style.backgroundColor = "var(--accent-blue)";
		} else if (totalMins >= 2.5 && totalMins <= 5.5) {
			displaySpeechZone.textContent = "✅ Durata Ideale (3–5 minuti)";
			displaySpeechZone.style.color = "#15803d";
			speechGaugeBar.style.backgroundColor = "#16a34a";
		} else if (totalMins > 5.5 && totalMins <= 7.0) {
			displaySpeechZone.textContent = "⚠️ Leggermente lungo (Taglia dettagli minori)";
			displaySpeechZone.style.color = "#ca8a04";
			speechGaugeBar.style.backgroundColor = "#eab308";
		} else {
			displaySpeechZone.textContent = "🛑 Troppo lungo (Riduci del 30% per non rallentare la cucina)";
			displaySpeechZone.style.color = "var(--accent-red)";
			speechGaugeBar.style.backgroundColor = "var(--accent-red)";
		}
	}

	document.querySelectorAll('.btn-load-template').forEach(btn => {
		btn.addEventListener('click', () => {
			const type = btn.getAttribute('data-type');
			if (sampleTemplates[type]) {
				speechEditor.value = sampleTemplates[type];
				calculateStats();
			}
		});
	});

	document.getElementById('btn-clear-speech').addEventListener('click', () => {
		speechEditor.value = '';
		calculateStats();
	});

	speechEditor.addEventListener('input', calculateStats);
	paceSelect.addEventListener('change', calculateStats);
	bufferSelect.addEventListener('change', calculateStats);

	// Stopwatch
	let timerInterval = null;
	let startTime = 0;
	let elapsedTime = 0;
	let isRunning = false;

	const stopwatchDisplay = document.getElementById('stopwatch-display');
	const btnTimerStart = document.getElementById('btn-timer-start');
	const btnTimerPause = document.getElementById('btn-timer-pause');
	const btnTimerReset = document.getElementById('btn-timer-reset');

	function formatStopwatch(ms) {
		const totalSec = Math.floor(ms / 1000);
		const m = Math.floor(totalSec / 60).toString().padStart(2, '0');
		const s = (totalSec % 60).toString().padStart(2, '0');
		const tenth = Math.floor((ms % 1000) / 100);
		return `${m}:${s}.${tenth}`;
	}

	btnTimerStart.addEventListener('click', () => {
		if (!isRunning) {
			isRunning = true;
			startTime = Date.now() - elapsedTime;
			timerInterval = setInterval(() => {
				elapsedTime = Date.now() - startTime;
				stopwatchDisplay.textContent = formatStopwatch(elapsedTime);
			}, 100);
			btnTimerStart.style.display = 'none';
			btnTimerPause.style.display = 'inline-block';
		}
	});

	btnTimerPause.addEventListener('click', () => {
		if (isRunning) {
			isRunning = false;
			clearInterval(timerInterval);
			btnTimerStart.style.display = 'inline-block';
			btnTimerStart.textContent = '▶️ Riprendi';
			btnTimerPause.style.display = 'none';
		}
	});

	btnTimerReset.addEventListener('click', () => {
		isRunning = false;
		clearInterval(timerInterval);
		elapsedTime = 0;
		stopwatchDisplay.textContent = '00:00.0';
		btnTimerStart.style.display = 'inline-block';
		btnTimerStart.textContent = '▶️ Avvia Prova';
		btnTimerPause.style.display = 'none';
	});

	// Word Doc Export
	document.getElementById('btn-export-doc').addEventListener('click', () => {
		const text = speechEditor.value.trim() || 'Nessun testo inserito.';
		const paragraphs = text.split(/\n+/).filter(p => p.trim().length > 0);

		let pTags = '';
		paragraphs.forEach((p, idx) => {
			pTags += `<p style="font-size:16pt; line-height:1.7; margin-bottom:18pt; color:#111;"><strong>[${idx + 1}]</strong> ${p}</p>`;
		});

		const html = `
			<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:w="urn:schemas-microsoft-com:office:word" xmlns="http://www.w3.org/TR/REC-html40">
			<head><meta charset="utf-8"><title>Discorso di Matrimonio</title></head>
			<body style="font-family: Calibri, Arial, sans-serif; padding: 30px; font-size: 16pt;">
				<h1 style="color:#1e3a8a;">Manage.Wedding — Bozza Discorso</h1>
				<p style="font-size:11pt; color:#666;">Durata stimata: ${displayTimeResult.textContent} | Parole: ${displayWordCount.textContent}</p>
				${pTags}
			</body></html>
		`;

		const blob = new Blob(['\ufeff', html], { type: 'application/msword' });
		const url = URL.createObjectURL(blob);
		const a = document.createElement('a');
		a.href = url;
		a.download = `discorso-matrimonio-${new Date().toISOString().slice(0, 10)}.doc`;
		document.body.appendChild(a);
		a.click();
		document.body.removeChild(a);
		URL.revokeObjectURL(url);
	});

	// PDF Print
	document.getElementById('btn-export-pdf').addEventListener('click', () => {
		document.getElementById('print-est-time').textContent = displayTimeResult.textContent;
		document.getElementById('print-word-count').textContent = `${displayWordCount.textContent} parole`;

		const grid = document.getElementById('print-cue-cards-grid');
		grid.innerHTML = '';

		const text = speechEditor.value.trim() || 'Nessun testo inserito.';
		const paragraphs = text.split(/\n+/).filter(p => p.trim().length > 0);

		for (let i = 0; i < paragraphs.length; i += 2) {
			const cardNum = Math.floor(i / 2) + 1;
			const totalCards = Math.ceil(paragraphs.length / 2);

			const card = document.createElement('div');
			card.style.cssText = `border: 2px solid #000; padding: 25px; page-break-inside: avoid; font-family: 'Times New Roman', serif; background: #fff; margin-bottom: 20px;`;
			
			let inner = `<div style="display:flex; justify-content:space-between; border-bottom:1px solid #000; padding-bottom:5px; margin-bottom:15px; font-size:12px; font-weight:bold;"><span>SCHEDA ${cardNum} DI ${totalCards}</span><span>DURATA: ${displayTimeResult.textContent}</span></div>`;
			inner += `<p style="font-size:16px; line-height:1.6; margin-bottom:12px;"><strong>[${i + 1}]</strong> ${paragraphs[i]}</p>`;
			if (paragraphs[i + 1]) {
				inner += `<p style="font-size:16px; line-height:1.6; margin-bottom:0;"><strong>[${i + 2}]</strong> ${paragraphs[i + 1]}</p>`;
			}

			card.innerHTML = inner;
			grid.appendChild(card);
		}

		window.print();
	});

	// CSV Export
	document.getElementById('btn-export-csv').addEventListener('click', () => {
		let csv = "Metrica,Valore\n";
		csv += `"Tempo Stimato","${displayTimeResult.textContent}"\n`;
		csv += `"Conteggio Parole","${displayWordCount.textContent}"\n`;
		csv += `"Paragrafi","${displayParagraphCount.textContent}"\n`;
		csv += `"Velocita","${displayWpmRate.textContent} WPM"\n`;

		const blob = new Blob(['\ufeff' + csv], { type: 'text/csv;charset=utf-8;' });
		const url = URL.createObjectURL(blob);
		const a = document.createElement('a');
		a.href = url;
		a.download = `tempi-discorso-matrimonio.csv`;
		document.body.appendChild(a);
		a.click();
		document.body.removeChild(a);
		URL.revokeObjectURL(url);
	});

	speechEditor.value = sampleTemplates.bestman;
	calculateStats();
</script>
