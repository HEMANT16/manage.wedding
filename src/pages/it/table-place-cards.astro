---
import Layout from '../../layouts/Layout.astro';
import RelatedTools from '../../components/RelatedTools.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';

const schema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": ["WebApplication", "CalculatorApplication"],
      "@id": "https://manage.wedding/it/table-place-cards/#webapp",
      "name": "Generatore Segnaposto & Numeri Tavolo Matrimonio da Stampare",
      "url": "https://manage.wedding/it/table-place-cards",
      "inLanguage": "it",
      "description": "Generatore gratuito di segnaposto per matrimonio, cartoncini pieghevoli a cavalierino con icone menù e cartelli numeri tavolo per centrotavola pronti da stampare.",
      "applicationCategory": "DesignApplication",
      "operatingSystem": "All",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "EUR"
      }
    },
    {
      "@type": "HowTo",
      "name": "Come creare e stampare i segnaposto per il matrimonio",
      "description": "Istruzioni per comporre i cartellini da tavolo, inserire le intolleranze per i camerieri e stampare su cartoncino pregiato.",
      "inLanguage": "it",
      "step": [
        {
          "@type": "HowToStep",
          "name": "1. Scegliere il formato del segnaposto",
          "text": "Seleziona tra cavalierino pieghevole autoportante (9×5 cm piegato), cartoncino piatto o numero per centrotavola."
        },
        {
          "@type": "HowToStep",
          "name": "2. Assegnare nomi, tavoli e icone menù",
          "text": "Indica il nome dell'invitato, il tavolo di appartenenza e l'icona del menù (carne, pesce, vegetariano, celiachia)."
        },
        {
          "@type": "HowToStep",
          "name": "3. Stampare su cartoncino da 250–300 g/m²",
          "text": "Scarica il PDF impaginato pronto per la stampa fronte-retro su carta cotone o cartoncino martellato."
        }
      ]
    },
    {
      "@type": "FAQPage",
      "inLanguage": "it",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "Qual è la differenza tra segnatavolo e segnaposto?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Il segnatavolo è il cartello posizionato al centro del tavolo con il numero o il nome tematico (es. 'Tavolo 4'). Il segnaposto è il cartoncino personalizzato con il nome del singolo invitato posizionato sopra il tovagliolo o il piatto."
          }
        },
        {
          "@type": "Question",
          "name": "Che carta è consigliabile per stampare i cavalierini pieghevoli?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "È raccomandato un cartoncino con grammatura compresa tra 250 g/m² e 300 g/m² (es. carta Amalfi, carta martellata o cartoncino kraft) che garantisce stabilità e non si piega sul tovagliolo."
          }
        }
      ]
    }
  ]
};
---

<Layout 
	title="Generatore Segnaposto & Numeri Tavolo Matrimonio da Stampare | Manage.Wedding"
	description="Crea e stampa gratis eleganti segnaposto per matrimonio, cartoncini cavalierino pieghevoli, numeri per centrotavola ed esporta in PDF, Word e CSV con icone menù."
	schema={schema}
>
	<script is:inline src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>

	<main style="max-width: 1200px; margin: 0 auto; padding: clamp(0.75rem, 2.5vw, 2rem); box-sizing: border-box; overflow-x: hidden;">
		<Breadcrumb title="Segnaposto & Numeri Tavolo" category="stationery" />
		
		<!-- Hero & Header -->
		<section class="section-paper" style="margin-bottom: 1.5rem; text-align: center;">
			<div style="max-width: 900px; margin: 0 auto; padding: 2.5rem 1rem;">
				<div class="tape" style="margin: 0 auto 1rem;"></div>
				<h1 class="font-heading" style="font-size: clamp(32px, 5vw, 56px); color: var(--fg-pencil); line-height: 1.15; text-align: center; margin-bottom: 0.75rem;">
					Segnaposto <span class="accent-italic">& Numeri Tavolo da Stampare</span>
				</h1>
				<p class="font-body" style="font-size: 1.25rem; color: var(--fg-pencil); max-width: 680px; margin: 0 auto; text-align: center;">
					Genera cartoncini pieghevoli a cavalierino, segnaposto piatti e numeri per centrotavola. Includi <strong>icone per il catering</strong> ed esporta in <strong>PDF pronto per la stampa</strong> o <strong>Word (.doc)</strong>.
				</p>
			</div>
		</section>

		<!-- 3-Step Visual Quick Guide Card -->
		<div class="hand-card" style="margin-bottom: 2rem; padding: 1.25rem; background: var(--bg-paper); border: 2px solid var(--fg-pencil);">
			<h2 class="font-heading" style="font-size: 1.25rem; color: var(--fg-pencil); margin: 0 0 0.75rem 0; text-align: center; display: flex; align-items: center; justify-content: center; gap: 0.5rem;">
				<span>📖</span> Come Creare e Stampare i Segnaposto in 3 Passaggi
			</h2>
			<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1rem;">
				<div style="background: #fff; border: 1.5px solid var(--fg-pencil); border-radius: 4px; padding: 0.85rem;">
					<strong class="font-heading" style="display: block; color: var(--accent-red); font-size: 1rem; margin-bottom: 0.25rem;">
						1️⃣ Scegli Formato & Cornice
					</strong>
					<p class="font-body" style="font-size: 0.85rem; margin: 0; line-height: 1.4; color: var(--fg-pencil);">
						Scegli tra cartoncini pieghevoli a cavalierino (9×5 cm piegati), segnaposto piatti o cartelli grandi numeri centrotavola.
					</p>
				</div>

				<div style="background: #fff; border: 1.5px solid var(--fg-pencil); border-radius: 4px; padding: 0.85rem;">
					<strong class="font-heading" style="display: block; color: var(--accent-blue); font-size: 1rem; margin-bottom: 0.25rem;">
						2️⃣ Assegna Tavoli & Icone Menù
					</strong>
					<p class="font-body" style="font-size: 0.85rem; margin: 0; line-height: 1.4; color: var(--fg-pencil);">
						Inserisci i nomi degli invitati e il tavolo, oppure clicca <strong>"✨ Carica Tavoli Esempio"</strong>. Attiva le icone (Carne, Pesce, Veg, Gluten-Free) per facilitare il servizio dei camerieri.
					</p>
				</div>

				<div style="background: #fff; border: 1.5px solid var(--fg-pencil); border-radius: 4px; padding: 0.85rem;">
					<strong class="font-heading" style="display: block; color: #16a34a; font-size: 1rem; margin-bottom: 0.25rem;">
						3️⃣ Esporta PDF & Lista Catering
					</strong>
					<p class="font-body" style="font-size: 0.85rem; margin: 0; line-height: 1.4; color: var(--fg-pencil);">
						Clicca su <strong>"🖨️ Stampa PDF Segnaposto"</strong> per ottenere i fogli impaginati da ritagliare, oppure <strong>"📝 Esporta Word (.doc)"</strong> per il maître di sala.
					</p>
				</div>
			</div>
		</div>

		<!-- Main Workspace Grid -->
		<div style="display: grid; grid-template-columns: 1fr; lg:grid-template-columns: 1.05fr 0.95fr; gap: 2rem; align-items: start; margin-bottom: 3.5rem;" class="placecards-calc-grid">
			
			<!-- Left Column: Card Configuration & Table Manager -->
			<div class="hand-card" style="padding: 1.75rem; background: #fff;">
				
				<div style="margin-bottom: 1.25rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.75rem;">
					<h2 class="font-heading" style="font-size: 1.4rem; color: var(--fg-pencil); margin: 0; text-align: center;">
						🏷️ Grafica, Formato & Opzioni
					</h2>
					<span class="font-body" style="font-size: 0.85rem; color: var(--text-secondary); display: block; text-align: center;">
						Personalizza lo stile del cartoncino e la composizione dei tavoli
					</span>
				</div>

				<form id="placecards-form" style="display: flex; flex-direction: column; gap: 1rem;">
					
					<!-- Card Type & Border Theme -->
					<div style="display: grid; grid-template-columns: 1.2fr 1fr; gap: 0.6rem;">
						<div>
							<label for="select-card-type" class="font-heading" style="display: block; font-size: 0.9rem; color: var(--accent-red); margin-bottom: 0.25rem;">
								Formato Cartoncino *
							</label>
							<select id="select-card-type" style="width: 100%; box-sizing: border-box; padding: 0.5rem; border: 2px solid var(--fg-pencil); border-radius: 4px; background: #fff; font-family: var(--font-body); font-size: 0.9rem;">
								<option value="tent" selected>🏷️ Cavalierini Pieghevoli (9×5 cm piegati)</option>
								<option value="flat">🎴 Segnaposto Piatti (9×5 cm)</option>
								<option value="tablenum">🔢 Cartelli Centrotavola (13×18 cm / 5×7")</option>
							</select>
						</div>

						<div>
							<label for="select-border-theme" class="font-heading" style="display: block; font-size: 0.9rem; color: var(--accent-blue); margin-bottom: 0.25rem;">
								Tema Cornice & Fregi
							</label>
							<select id="select-border-theme" style="width: 100%; box-sizing: border-box; padding: 0.5rem; border: 2px solid var(--fg-pencil); border-radius: 4px; background: #fff; font-family: var(--font-body); font-size: 0.9rem;">
								<option value="botanical" selected>🌿 Bordo Botanico Floreale</option>
								<option value="classic">✒️ Classico Minimale a Doppio Filetto</option>
								<option value="crest">🏛️ Elegante con Angoli Decorati</option>
							</select>
						</div>
					</div>

					<!-- Options: Meal Icons & Table Number Badges -->
					<div style="background: var(--bg-paper); border: 1.5px dashed var(--fg-pencil); border-radius: 4px; padding: 0.75rem; display: flex; flex-direction: column; gap: 0.5rem;">
						<span class="font-heading" style="font-size: 0.9rem; color: var(--fg-pencil); display: block;">
							⚙️ Elementi Visivi del Segnaposto
						</span>
						<div style="display: flex; gap: 1rem; flex-wrap: wrap;">
							<label style="font-size: 0.85rem; display: flex; align-items: center; gap: 0.35rem; cursor: pointer;">
								<input type="checkbox" id="chk-show-meal-icons" checked style="accent-color: var(--fg-pencil);">
								<span>Mostra Icone Menù Catering (🥩 / 🐟 / 🥦)</span>
							</label>
							<label style="font-size: 0.85rem; display: flex; align-items: center; gap: 0.35rem; cursor: pointer;">
								<input type="checkbox" id="chk-show-table-badge" checked style="accent-color: var(--fg-pencil);">
								<span>Mostra Numero del Tavolo sul Segnaposto</span>
							</label>
						</div>
					</div>

					<!-- Quick Add Guest & Table Entry -->
					<div style="background: var(--bg-paper); border: 1.5px solid var(--fg-pencil); border-radius: 4px; padding: 0.85rem; display: flex; flex-direction: column; gap: 0.6rem;">
						<div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.4rem;">
							<span class="font-heading" style="font-size: 0.9rem; color: var(--fg-pencil);">
								👥 Elenco Assegnazioni: <strong id="assigned-count-badge">0 Ospiti</strong>
							</span>
							<div style="display: flex; gap: 0.3rem;">
								<button type="button" id="btn-load-samples" class="hand-btn" style="padding: 0.25rem 0.55rem; font-size: 0.78rem; background: #eff6ff;">
									✨ Carica Esempio
								</button>
								<button type="button" id="btn-clear-all" class="hand-btn secondary" style="padding: 0.25rem 0.55rem; font-size: 0.78rem; color: #b91c1c;">
									🗑️ Svuota
								</button>
							</div>
						</div>

						<div style="display: grid; grid-template-columns: 1.3fr 0.8fr 0.9fr auto; gap: 0.35rem; align-items: center;">
							<input type="text" id="input-new-guest-name" placeholder="Nome Invitato" style="padding: 0.4rem; border: 1px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem;">
							<input type="text" id="input-new-table-num" placeholder="Tavolo #" value="1" style="padding: 0.4rem; border: 1px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem;">
							<select id="select-new-meal" style="padding: 0.4rem; border: 1px solid var(--fg-pencil); border-radius: 4px; font-size: 0.85rem; background: #fff;">
								<option value="🥩 Carne">🥩 Carne</option>
								<option value="🐟 Pesce">🐟 Pesce</option>
								<option value="🥦 Vegetariano">🥦 Veg</option>
								<option value="🌾 Senza Glutine">🌾 Gluten-Free</option>
							</select>
							<button type="button" id="btn-add-guest" class="hand-btn" style="padding: 0.4rem 0.65rem; font-size: 0.85rem;">
								+ Aggiungi
							</button>
						</div>
					</div>

				</form>
			</div>

			<!-- Right Column: Live Card Preview & Table Cards Stream -->
			<div style="display: flex; flex-direction: column; gap: 1.5rem;">
				
				<!-- Live Card Preview Card -->
				<div class="hand-card" style="padding: 1.75rem; background: #fff;">
					
					<div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 1.25rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.75rem;">
						<div style="width: 100%;">
							<h3 class="font-heading" style="font-size: 1.4rem; color: var(--fg-pencil); margin: 0; text-align: center;">
								🖼️ Anteprima Live Segnaposto
							</h3>
							<span class="font-body" style="font-size: 0.85rem; color: var(--text-secondary); display: block; text-align: center;">
								Scala standard 9×5 cm
							</span>
						</div>

						<!-- Exports Toolbar -->
						<div style="display: flex; gap: 0.4rem; flex-wrap: wrap; justify-content: center; width: 100%; margin-top: 0.5rem;">
							<button type="button" id="btn-export-doc" class="hand-btn" style="padding: 0.35rem 0.75rem; font-size: 0.85rem; background: #eff6ff;">
								📝 Esporta Word (.doc)
							</button>
							<button type="button" id="btn-export-pdf" class="hand-btn" style="padding: 0.35rem 0.75rem; font-size: 0.85rem; background: #fef2f2;">
								🖨️ Stampa PDF Segnaposto
							</button>
							<button type="button" id="btn-export-csv" class="hand-btn secondary" style="padding: 0.35rem 0.75rem; font-size: 0.85rem;">
								📊 CSV
							</button>
						</div>
					</div>

					<!-- Interactive Preview Place Card Frame -->
					<div id="preview-placecard-canvas" class="theme-botanical" style="border: 2px solid var(--fg-pencil); border-radius: 4px; padding: 1.75rem 1.25rem; background: #faf8f5; text-align: center; position: relative; box-shadow: 3px 3px 0px var(--fg-pencil); margin-bottom: 1.25rem;">
						<div style="position: absolute; top: 4px; left: 4px; width: 12px; height: 12px; border-top: 2px solid var(--fg-pencil); border-left: 2px solid var(--fg-pencil);"></div>
						<div style="position: absolute; top: 4px; right: 4px; width: 12px; height: 12px; border-top: 2px solid var(--fg-pencil); border-right: 2px solid var(--fg-pencil);"></div>
						<div style="position: absolute; bottom: 4px; left: 4px; width: 12px; height: 12px; border-bottom: 2px solid var(--fg-pencil); border-left: 2px solid var(--fg-pencil);"></div>
						<div style="position: absolute; bottom: 4px; right: 4px; width: 12px; height: 12px; border-bottom: 2px solid var(--fg-pencil); border-right: 2px solid var(--fg-pencil);"></div>

						<h2 id="prev-guest-name-display" class="font-heading" style="font-size: 1.6rem; color: var(--fg-pencil); margin: 0 0 0.3rem 0; line-height: 1.2;">
							Eleonora Bianchi
						</h2>

						<div style="width: 40px; height: 1.5px; background: var(--fg-pencil); margin: 0.4rem auto;"></div>

						<p id="prev-table-badge-display" class="font-body" style="font-size: 0.9rem; text-transform: uppercase; letter-spacing: 1.5px; color: var(--accent-red); margin: 0.3rem 0 0 0; font-weight: bold;">
							Tavolo 1
						</p>

						<div id="prev-meal-icon-display" style="position: absolute; bottom: 6px; right: 8px; font-size: 1.1rem;" title="Scelta Menù">
							🥩
						</div>
					</div>

					<button type="button" id="btn-download-sample-png" class="hand-btn" style="width: 100%; text-align: center; font-size: 0.95rem; padding: 0.55rem; background: #f0fdf4;">
						📸 Scarica Immagine Segnaposto (PNG)
					</button>

				</div>

				<!-- Table Roster & Group Breakdown Stream -->
				<div class="hand-card" style="padding: 1.5rem; background: #fff; max-height: 400px; overflow-y: auto;">
					<h3 class="font-heading" style="font-size: 1.15rem; color: var(--fg-pencil); margin: 0 0 0.75rem 0; text-align: center;">
						📋 Invitati Assegnati per Tavolo
					</h3>
					<div id="table-cards-stream" style="display: flex; flex-direction: column; gap: 0.75rem;"></div>
				</div>

			</div>

		</div>

		<!-- Hidden Printable Sheet for PDF Generation -->
		<div id="printable-pdf-sheet" class="print-only">
			<div style="border-bottom: 2px solid #000; padding-bottom: 8px; margin-bottom: 15px; text-align: center;">
				<h1 style="font-size: 22px; margin: 0 0 4px 0;">Segnaposto Ufficiali Matrimonio</h1>
				<p style="margin: 0; font-size: 12px; color: #555;">Data: <span id="print-date"></span> | Totale Segnaposto: <span id="print-guest-count"></span></p>
			</div>
			<div id="print-cards-grid" style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;"></div>
		</div>

		<!-- SEO Article -->
		<article class="hand-card" style="padding: clamp(1.5rem, 4vw, 3.5rem); margin-bottom: 4rem;">
			<div style="text-align: center; margin-bottom: 2.5rem;">
				<span class="badge-postit">Guida alla Cancelleria di Nozze</span>
				<h2 class="font-heading" style="font-size: clamp(28px, 4vw, 42px); color: var(--fg-pencil); margin-top: 0.5rem; text-align: center;">
					Segnaposto e Segnatavoli: Galateo & Stampa Fai da Te
				</h2>
				<p class="font-body" style="font-size: 1.2rem; color: var(--text-secondary); max-width: 750px; margin: 0.5rem auto 0; text-align: center;">
					Grammatura della carta, icone per il personale di sala e coordinamento con il Tableau de Mariage.
				</p>
			</div>

			<section style="margin-bottom: 3rem;">
				<h3 class="font-heading" style="font-size: 2rem; color: var(--fg-pencil); margin-bottom: 1rem; border-bottom: 2px dashed var(--fg-pencil); padding-bottom: 0.5rem; text-align: center;">
					1. La Scelta della Carta e della Grammatura
				</h3>
				<p class="font-body" style="line-height: 1.7; margin-bottom: 1.25rem;">
					Per i cavalierini pieghevoli è fondamentale utilizzare cartoncino pesante con grammatura da 250 a 300 g/m² (ad esempio carta vergata o cartoncino martellato avorio). Una carta troppo leggera tende ad aprirsi e piegarsi sotto il peso dell'umidità o dell'aria condizionata della sala.
				</p>
			</section>
		</article>

		<RelatedTools currentPath="/it/table-place-cards" category="stationery" />
	</main>
</Layout>

<style>
	.theme-botanical {
		background: #faf8f5 !important;
		border-color: #2d5a27 !important;
	}
	.theme-classic {
		background: #ffffff !important;
		border-color: #000000 !important;
	}
	.theme-crest {
		background: #fdfaf6 !important;
		border-color: #92400e !important;
	}

	.print-only {
		display: none;
	}

	@media print {
		body * {
			visibility: hidden;
		}
		.print-only, .print-only * {
			visibility: visible;
		}
		.print-only {
			display: block !important;
			position: absolute;
			left: 0;
			top: 0;
			width: 100%;
			background: #fff;
			color: #000;
			padding: 15px;
		}
	}
</style>

<script is:inline>
	let seatedGuestList = [];

	const selectCardType = document.getElementById('select-card-type');
	const selectBorderTheme = document.getElementById('select-border-theme');
	const chkShowMealIcons = document.getElementById('chk-show-meal-icons');
	const chkShowTableBadge = document.getElementById('chk-show-table-badge');

	const prevGuestNameDisplay = document.getElementById('prev-guest-name-display');
	const prevTableBadgeDisplay = document.getElementById('prev-table-badge-display');
	const prevMealIconDisplay = document.getElementById('prev-meal-icon-display');
	const previewCanvas = document.getElementById('preview-placecard-canvas');

	const assignedCountBadge = document.getElementById('assigned-count-badge');
	const tableCardsStream = document.getElementById('table-cards-stream');

	function loadSeatedGuests() {
		try {
			const saved = localStorage.getItem('manage_wedding_guests_it');
			if (saved) {
				const all = JSON.parse(saved);
				seatedGuestList = all.filter(g => g.table && g.table.trim() !== "");
			}
		} catch (e) {
			seatedGuestList = [];
		}

		if (!seatedGuestList || seatedGuestList.length === 0) {
			loadSampleTables();
			return;
		}
		renderPlaceCards();
	}

	function loadSampleTables() {
		seatedGuestList = [
			{ name: "Eleonora Bianchi", table: "1", meal: "🥩 Carne" },
			{ name: "Marco Rossi", table: "1", meal: "🐟 Pesce" },
			{ name: "Marta De Luca", table: "Tavolo d'Onore", meal: "🥦 Vegetariano" },
			{ name: "Giacomo Ferri", table: "2", meal: "🥩 Carne" },
			{ name: "Sara Moretti", table: "2", meal: "🌾 Senza Glutine" },
			{ name: "Nonna Caterina", table: "Tavolo 1", meal: "🐟 Pesce" },
			{ name: "Zio Roberto", table: "Tavolo 1", meal: "🥩 Carne" }
		];
		renderPlaceCards();
	}

	function renderPlaceCards() {
		const cardType = selectCardType.value;
		const theme = selectBorderTheme.value;
		const showMeal = chkShowMealIcons.checked;
		const showTable = chkShowTableBadge.checked;

		previewCanvas.className = `theme-${theme}`;
		
		if (cardType === 'tablenum') {
			prevGuestNameDisplay.textContent = "Numero Tavolo";
			prevTableBadgeDisplay.textContent = "TAVOLO 1";
			prevTableBadgeDisplay.style.fontSize = "1.8rem";
			prevMealIconDisplay.style.display = "none";
		} else {
			if (seatedGuestList.length > 0) {
				prevGuestNameDisplay.textContent = seatedGuestList[0].name;
				prevTableBadgeDisplay.textContent = `Tavolo ${seatedGuestList[0].table}`;
				prevTableBadgeDisplay.style.fontSize = "0.9rem";
				prevMealIconDisplay.textContent = seatedGuestList[0].meal.split(' ')[0] || '🥩';
			} else {
				prevGuestNameDisplay.textContent = "Nome Invitato";
				prevTableBadgeDisplay.textContent = "Tavolo 1";
				prevMealIconDisplay.textContent = "🥩";
			}
			prevTableBadgeDisplay.style.display = showTable ? "block" : "none";
			prevMealIconDisplay.style.display = showMeal ? "block" : "none";
		}

		assignedCountBadge.textContent = `${seatedGuestList.length} Ospiti`;

		const grouped = {};
		seatedGuestList.forEach(g => {
			const tbl = g.table || 'Non Assegnato';
			if (!grouped[tbl]) grouped[tbl] = [];
			grouped[tbl].push(g);
		});

		tableCardsStream.innerHTML = '';
		if (seatedGuestList.length === 0) {
			tableCardsStream.innerHTML = `
				<div style="text-align: center; padding: 1rem; color: var(--text-secondary); font-style: italic; font-size: 0.9rem;">
					Nessun ospite assegnato. Clicca su "✨ Carica Esempio" per iniziare!
				</div>
			`;
			return;
		}

		Object.keys(grouped).forEach(tbl => {
			const groupBlock = document.createElement('div');
			groupBlock.style.cssText = `background: var(--bg-paper); border: 1.5px solid var(--fg-pencil); border-radius: 4px; padding: 0.75rem;`;
			
			let listHtml = '';
			grouped[tbl].forEach((g) => {
				listHtml += `
					<div style="display: flex; justify-content: space-between; align-items: center; padding: 0.25rem 0; border-bottom: 1px dotted #ccc; font-size: 0.88rem;">
						<span>${g.name} <small style="color:#666;">(${g.meal || 'Menù Standard'})</small></span>
						<button type="button" class="btn-del-seat hand-btn secondary" data-name="${g.name}" style="padding: 0.15rem 0.4rem; font-size: 0.7rem; color: #b91c1c;">🗑️</button>
					</div>
				`;
			});

			groupBlock.innerHTML = `
				<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.4rem;">
					<strong class="font-heading" style="color: var(--accent-red); font-size: 1rem;">Tavolo: ${tbl}</strong>
					<span style="font-size: 0.75rem; color: var(--text-secondary); font-weight: bold;">${grouped[tbl].length} Ospiti</span>
				</div>
				<div>${listHtml}</div>
			`;
			tableCardsStream.appendChild(groupBlock);
		});

		document.querySelectorAll('.btn-del-seat').forEach(btn => {
			btn.addEventListener('click', () => {
				const gName = btn.getAttribute('data-name');
				seatedGuestList = seatedGuestList.filter(g => g.name !== gName);
				renderPlaceCards();
			});
		});
	}

	document.getElementById('btn-download-sample-png').addEventListener('click', () => {
		if (window.html2canvas) {
			window.html2canvas(previewCanvas, {
				scale: 3,
				backgroundColor: null,
				useCORS: true
			}).then(canvas => {
				const link = document.createElement('a');
				link.download = `Segnaposto_Esempio.png`;
				link.href = canvas.toDataURL("image/png");
				link.click();
			});
		} else {
			alert("Inizializzazione grafica in corso. Riprova tra 2 secondi.");
		}
	});

	document.getElementById('btn-add-guest').addEventListener('click', () => {
		const nameInput = document.getElementById('input-new-guest-name');
		const tblInput = document.getElementById('input-new-table-num');
		const mealSelect = document.getElementById('select-new-meal');

		const name = nameInput.value.trim();
		const table = tblInput.value.trim() || '1';
		const meal = mealSelect.value;

		if (name) {
			seatedGuestList.push({ name, table, meal });
			nameInput.value = '';
			renderPlaceCards();
		}
	});

	document.getElementById('btn-load-samples').addEventListener('click', loadSampleTables);

	document.getElementById('btn-clear-all').addEventListener('click', () => {
		seatedGuestList = [];
		renderPlaceCards();
	});

	[selectCardType, selectBorderTheme, chkShowMealIcons, chkShowTableBadge].forEach(el => {
		el.addEventListener('change', renderPlaceCards);
	});

	document.getElementById('btn-export-doc').addEventListener('click', () => {
		const grouped = {};
		seatedGuestList.forEach(g => {
			const tbl = g.table || 'Non Assegnato';
			if (!grouped[tbl]) grouped[tbl] = [];
			grouped[tbl].push(g);
		});

		let tablesHtml = '';
		Object.keys(grouped).forEach(tbl => {
			let rows = '';
			grouped[tbl].forEach(g => {
				rows += `<tr><td>${g.name}</td><td>${g.meal || 'Standard'}</td></tr>`;
			});

			tablesHtml += `
				<h3 style="color:#1e3a8a; margin-top:20px; border-bottom:1px solid #ccc; padding-bottom:4px;">Tavolo: ${tbl} (${grouped[tbl].length} Ospiti)</h3>
				<table style="width:100%; border-collapse:collapse; margin-bottom:15px; font-size:12px;">
					<thead><tr style="background:#f2f2f2;"><th style="border:1px solid #ccc; padding:6px; text-align:left;">Nome Invitato</th><th style="border:1px solid #ccc; padding:6px; text-align:left;">Scelta Menù</th></tr></thead>
					<tbody>${rows}</tbody>
				</table>
			`;
		});

		const html = `
			<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:w="urn:schemas-microsoft-com:office:word" xmlns="http://www.w3.org/TR/REC-html40">
			<head>
				<meta charset="utf-8">
				<title>Riepilogo Segnaposto e Menù Matrimonio</title>
				<style>
					body { font-family: Arial, sans-serif; padding: 20px; }
					table { width: 100%; border-collapse: collapse; margin-top: 10px; font-size: 13px; }
					th { background-color: #f2f2f2; border: 1px solid #ccc; padding: 8px; text-align: left; }
					td { border: 1px solid #ccc; padding: 8px; }
				</style>
			</head>
			<body>
				<h1>Manage.Wedding — Riepilogo Ufficiale Segnaposto e Menù</h1>
				<p>Generato il ${new Date().toLocaleDateString('it-IT')} | Totale Ospiti: <strong>${seatedGuestList.length}</strong></p>
				${tablesHtml}
			</body>
			</html>
		`;

		const blob = new Blob(['\ufeff', html], { type: 'application/msword' });
		const url = URL.createObjectURL(blob);
		const a = document.createElement('a');
		a.href = url;
		a.download = `Riepilogo_Segnaposto_${new Date().toISOString().slice(0, 10)}.doc`;
		document.body.appendChild(a);
		a.click();
		document.body.removeChild(a);
		URL.revokeObjectURL(url);
	});

	document.getElementById('btn-export-pdf').addEventListener('click', () => {
		document.getElementById('print-date').textContent = new Date().toLocaleDateString('it-IT');
		document.getElementById('print-guest-count').textContent = seatedGuestList.length;

		const grid = document.getElementById('print-cards-grid');
		grid.innerHTML = '';

		const showMeal = chkShowMealIcons.checked;
		const showTable = chkShowTableBadge.checked;

		seatedGuestList.forEach(g => {
			const card = document.createElement('div');
			card.style.cssText = `border: 2px solid #000; padding: 25px 15px; text-align: center; font-family: 'Times New Roman', serif; position: relative; height: 130px; box-sizing: border-box;`;
			card.innerHTML = `
				<h2 style="font-size: 20px; margin: 10px 0 5px 0;">${g.name}</h2>
				<div style="width: 30px; height: 1px; background: #000; margin: 5px auto;"></div>
				${showTable ? `<p style="font-size: 13px; font-weight: bold; margin: 5px 0 0 0; text-transform: uppercase;">Tavolo ${g.table}</p>` : ''}
				${showMeal ? `<span style="position: absolute; bottom: 8px; right: 10px; font-size: 16px;">${g.meal.split(' ')[0] || '🥩'}</span>` : ''}
			`;
			grid.appendChild(card);
		});

		window.print();
	});

	document.getElementById('btn-export-csv').addEventListener('click', () => {
		let csv = "Nome Invitato,Numero Tavolo,Menu\n";
		seatedGuestList.forEach(g => {
			csv += `"${g.name}","Tavolo ${g.table}","${g.meal || 'Standard'}"\n`;
		});

		const blob = new Blob(['\ufeff' + csv], { type: 'text/csv;charset=utf-8;' });
		const url = URL.createObjectURL(blob);
		const a = document.createElement('a');
		a.href = url;
		a.download = `Segnaposto_${new Date().toISOString().slice(0, 10)}.csv`;
		document.body.appendChild(a);
		a.click();
		document.body.removeChild(a);
		URL.revokeObjectURL(url);
	});

	loadSeatedGuests();
</script>
